# monitor-shared — agent.md

## Propósito
Tipos comuns compartilhados entre o `monitor-agent` (que escreve no DB) e o launcher (que lê do DB). Mantém o schema da tabela `metrics` versionado em código e evita drift entre escritor e leitor.

## Arquivos
- `Cargo.toml` — manifesto do crate (lib).
- `src/lib.rs` — `MetricRow`, `MetricSource` (variants `Vm`/`Container`/`Hetzner`/`Vercel`), `VercelDeployment`, `HOST_NAME`, `POLL_INTERVAL_SECS`, `HEALTH_ENDPOINTS`.

## Padrões
- Tipos serializáveis (`serde::Serialize` + `Deserialize`) pra inserção em Postgres e transporte JSON quando necessário.
- Constantes que tanto o agente quanto o launcher consomem vivem aqui (não duplicar em Cargo.toml).
- Sem dependências pesadas — só `serde`, `chrono` e o mínimo necessário.

## Decisões recentes
- 2026-05-06: criado junto com a Fase 1 do monitor.
- 2026-05-07 (Sprint 2 — Vercel stacks): `MetricSource` ganhou variant `Vercel`. Struct nova `VercelDeployment` (14 campos: project_id/name, deployment_id, state, url, prod_url, branch, commit_sha/msg, author, created_at, ready_at, build_ms) compartilhada entre o coletor Vercel e o INSERT em `vercel_deployments`.
- 2026-05-08 (Sprint B3 — Custos multi-serviço): adicionado `ExternalMetric { ts, service, metric, value, quota, unit, period_start }`. Heterogêneo demais pra `MetricRow` (carrega quota+period_start). Usado pelos coletores `vercel_usage`, `gh_actions` e espelhamento `hetzner`. Agente bumped 0.2.0 → 0.3.0.

## Pendências conhecidas
- Nenhuma.
