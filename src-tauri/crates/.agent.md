# crates/ — agent.md

## Propósito
Subcrates do workspace Cargo do `falcao-launcher`, para componentes que precisam ser construídos como bins/libs separados (ex: agente de monitoramento que roda em outra máquina).

## Arquivos
- `monitor-shared/` — tipos comuns entre launcher e agente (MetricRow, MetricSource, constantes). Ver `.agent.md` da pasta.
- `monitor-agent/` — binário do agente coletor 24/7 que roda na VM Hetzner. Ver `.agent.md` da pasta.

## Padrões
- Cada subcrate tem `Cargo.toml` próprio referenciando workspace deps via `{ workspace = true }`.
- Versões/edition unificadas no `[workspace.package]` do `src-tauri/Cargo.toml`.
- Subcrates podem depender uns dos outros via `{ path = "../monitor-shared" }`.

## Decisões recentes
- 2026-05-06: workspace criado pra suportar multi-binário (launcher Tauri + agente standalone). Spec `docs/superpowers/specs/2026-05-06-vm-monitor-fase-1-design.md`, plan `docs/superpowers/plans/2026-05-06-vm-monitor-fase-1.md`.

## Pendências conhecidas
- Nenhuma.
