# Sprint B1 — Snyk-like (vulnerabilidades cross-repo + imagens Docker) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Adicionar feature de monitoramento de vulnerabilidades (CVEs) cross-repo + imagens Docker da VM. Aba "Segurança" no launcher com lista filtrada, dismiss persistente, on-demand scan + cron diário. Fontes: Dependabot alerts + Trivy + GHSA cross-cutting.

**Architecture:** Coleta hybrid — Dependabot/GHSA via GH Actions cron (reusa SSH key dedicada `falcao-monitor-push` da Sprint 2), Trivy via systemd timer na VM. Tabela única `vulnerabilities` heterogênea (kind discriminator) no Postgres TimescaleDB. Aba "Segurança" no topbar com filtros e dismiss; chip vermelho discreto nos `ProjectCard`s da aba Projetos.

**Tech Stack:** Tauri 2 + React 19 + TS + Postgres TimescaleDB + bash + GH Actions cron + systemd user services + Trivy (binário). Sem deps frontend novas.

**Spec source:** `docs/superpowers/specs/2026-05-08-snyk-like-design.md`

**Branch:** `feature/snyk-like` (já criada, com spec commitado).

---

## File structure

Arquivos novos e modificados, organizados por responsabilidade.

### DB (Phase A1)
- **Create:** `docs/superpowers/vm-migrations/008_vulnerabilities.sql`

### Coleta Dependabot/GHSA (Phase A2)
- **Create:** `scripts/scan-dependabot.sh` — busca alerts cross-repo + advisories GHSA
- **Create:** `scripts/push-vulnerabilities.sh` — pipe CSV → SSH → psql `\COPY` (variação do `push-health-results.sh` existente)
- **Create:** `.github/workflows/security-scan.yml` — cron diário + workflow_dispatch

### Coleta Trivy (Phase A3 — manual + scripts na VM)
- **Create na VM:** `/home/falcao/.local/bin/scan-trivy.sh`
- **Create na VM:** `~/.config/systemd/user/falcao-trivy-scanner.service`
- **Create na VM:** `~/.config/systemd/user/falcao-trivy-scanner.timer`
- **Edit na VM:** `~/.config/falcao-monitor/.env` — adiciona `GITHUB_PAT_SECURITY=...`

### Backend Rust (Phase B — paralelizável com C)
- **Create:** `src-tauri/src/monitor/security.rs` — queries SELECT + tipos
- **Modify:** `src-tauri/src/monitor/mod.rs` — re-export `security`
- **Modify:** `src-tauri/src/monitor/commands.rs` — 6 comandos novos
- **Modify:** `src-tauri/src/external.rs` — `trigger_trivy_scan_on_vm`, `trigger_dependabot_scan_via_gh`
- **Modify:** `src-tauri/src/config.rs` — `DismissedVuln` + `dismissed_vulnerabilities` field
- **Modify:** `src-tauri/src/lib.rs` — registra comandos novos no `generate_handler!`

### Frontend (Phase C — paralelizável com B após C1)
- **Create:** `src/types/security.ts` — `VulnSeverity`, `VulnKind`, `VulnerabilityRow`, `VulnSummary`, `VulnFilters`
- **Create:** `src/components/SecurityTab.tsx` — orquestrador (~250 linhas)
- **Create:** `src/components/VulnerabilityRow.tsx` — item da lista
- **Create:** `src/components/SecurityChip.tsx` — chip compacto pros ProjectCards
- **Create:** `src/components/SecurityScanProgress.tsx` — overlay durante scan on-demand
- **Modify:** `src/lib/monitor.ts` — 6 wrappers novos (listVulnerabilities, vulnSummary, vulnCountByRepo, triggerScan, dismissCve, undismissCve)
- **Modify:** `src/App.tsx` — `TopView` ganha `"security"`, botão na topbar, state `vulnCountByRepo`
- **Modify:** `src/components/ProjectCard.tsx` — render `<SecurityChip count={vulnCount}>`

### Docs (Phase E)
- Atualizar 4 agent.md (`src-tauri/src/`, `src-tauri/src/monitor/`, `src/components/`, `src/lib/`, `src/types/`)
- Atualizar CLAUDE.md (seção Sprint B1)
- Atualizar VALIDATION.md (seção Sprint B1)
- Atualizar skill `falcao-launcher` (sessão 12)

---

## Phase A — DB + scripts

### Task A1: Migration `008_vulnerabilities.sql`

**Files:**
- Create: `docs/superpowers/vm-migrations/008_vulnerabilities.sql`

- [ ] **Step 1: Verificar branch + criar migration**

```bash
git branch --show-current  # deve ser feature/snyk-like
```

Cria `docs/superpowers/vm-migrations/008_vulnerabilities.sql`:

```sql
-- Tabela única heterogênea pra vulnerabilidades (Sprint B1 — Snyk-like).
-- Discriminator `kind`: 'deps' (Dependabot), 'image' (Trivy), 'advisory' (GHSA).
-- Hypertable TimescaleDB pra retention/compression uniforme com o resto.
CREATE TABLE IF NOT EXISTS vulnerabilities (
  ts            TIMESTAMPTZ NOT NULL,
  kind          TEXT        NOT NULL,
  severity      TEXT        NOT NULL,
  cve_id        TEXT,
  ghsa_id       TEXT,
  source_id     TEXT        NOT NULL,
  package_name  TEXT,
  package_version TEXT,
  fix_version   TEXT,
  title         TEXT,
  url           TEXT,
  state         TEXT        NOT NULL,
  raw           JSONB
);

SELECT create_hypertable('vulnerabilities', 'ts', if_not_exists => TRUE);

CREATE INDEX IF NOT EXISTS idx_vuln_lookup
  ON vulnerabilities (kind, severity, source_id, ts DESC);

CREATE INDEX IF NOT EXISTS idx_vuln_open_critical
  ON vulnerabilities (severity, ts DESC)
  WHERE state = 'open' AND severity IN ('critical', 'high');

ALTER TABLE vulnerabilities SET (
  timescaledb.compress,
  timescaledb.compress_segmentby = 'kind, source_id'
);

SELECT add_compression_policy('vulnerabilities', INTERVAL '7 days', if_not_exists => TRUE);
SELECT add_retention_policy('vulnerabilities', INTERVAL '90 days', if_not_exists => TRUE);

GRANT SELECT ON vulnerabilities TO monitor_reader;
GRANT INSERT, SELECT ON vulnerabilities TO monitor_writer;
```

- [ ] **Step 2: Copiar pra VM (igual migrations anteriores)**

```bash
scp docs/superpowers/vm-migrations/008_vulnerabilities.sql falcao@162.55.217.189:/opt/falcao-monitor/migrations/
ssh falcao@162.55.217.189 "ls -la /opt/falcao-monitor/migrations/008_vulnerabilities.sql"
```

Expected: arquivo na VM, ~800 bytes.

- [ ] **Step 3: Dry-run apply**

```bash
./scripts/apply-vm-migrations.sh --dry-run
```

Expected: `Pending migrations:` lista `008_vulnerabilities.sql`. Nenhuma execução.

- [ ] **Step 4: Aplicar migration**

```bash
./scripts/apply-vm-migrations.sh
```

Expected: `✓ applied 008_vulnerabilities.sql`.

- [ ] **Step 5: Validar tabela + grants**

```bash
ssh falcao@162.55.217.189 "docker exec falcao-monitor-db psql -U postgres -d falcao_monitor -c '\\d vulnerabilities' -c '\\dp vulnerabilities'"
```

Expected: 13 colunas listadas (ts...raw), 3 indexes (incluindo `vulnerabilities_ts_idx` automático do hypertable), grants `monitor_reader=r/monitor_writer=ar`.

- [ ] **Step 6: Commit**

```bash
git add docs/superpowers/vm-migrations/008_vulnerabilities.sql
git commit -m "feat(security): migration 008 — vulnerabilities hypertable

- Tabela única heterogênea (kind: deps|image|advisory)
- Hypertable TimescaleDB, retention 90d, compression 7d (segment_by kind+source_id)
- 2 indexes: lookup geral + parcial open-critical
- Grants alinhados com tabelas existentes (writer/reader)"
```

### Task A2: `scan-dependabot.sh` + workflow GH Actions

**Files:**
- Create: `scripts/scan-dependabot.sh`
- Create: `scripts/push-vulnerabilities.sh`
- Create: `.github/workflows/security-scan.yml`

- [ ] **Step 1: `scan-dependabot.sh`**

Cria `scripts/scan-dependabot.sh`:

```bash
#!/usr/bin/env bash
# Scaneia Dependabot alerts em todos os repos Falkzera/* + GHSA cross-cutting.
# Output: CSV em stdout. Header + N linhas (uma por CVE encontrado).
#
# Schema CSV: ts,kind,severity,cve_id,ghsa_id,source_id,package_name,package_version,fix_version,title,url,state
#
# Requer: gh CLI autenticada (GH_TOKEN no env CI, ou gh auth login local).

set -euo pipefail

NOW=$(date -u +"%Y-%m-%dT%H:%M:%SZ")

# CSV header
echo "ts,kind,severity,cve_id,ghsa_id,source_id,package_name,package_version,fix_version,title,url,state"

# 1. Dependabot alerts por repo Falkzera/*
REPOS=$(gh api /user/repos --paginate --jq '.[] | select(.owner.login=="Falkzera" and .archived == false) | .full_name')

# Coletar packages únicos pra GHSA query depois
declare -A SEEN_PACKAGES

for repo in $REPOS; do
  alerts_json=$(gh api "/repos/$repo/dependabot/alerts?state=open&per_page=100" --paginate 2>/dev/null) || {
    echo "[scan-dependabot] skip $repo: dependabot disabled or 404" >&2
    continue
  }

  echo "$alerts_json" | jq -r --arg ts "$NOW" --arg src "$repo" '
    .[] | [
      $ts,
      "deps",
      (.security_advisory.severity // "unknown"),
      (.security_advisory.cve_id // ""),
      (.security_advisory.ghsa_id // ""),
      $src,
      (.dependency.package.name // ""),
      (.security_vulnerability.vulnerable_version_range // ""),
      (.security_vulnerability.first_patched_version.identifier // ""),
      (.security_advisory.summary // ""),
      (.html_url // ""),
      "open"
    ] | @csv'

  # Acumula nomes de packages pra GHSA cross-check (ecosystem:name)
  while read -r pkg; do
    [[ -z "$pkg" || "$pkg" == "null" ]] && continue
    SEEN_PACKAGES["$pkg"]=1
  done < <(echo "$alerts_json" | jq -r '.[] | "\(.dependency.package.ecosystem):\(.dependency.package.name)"')
done

# 2. GHSA cross-cutting: top 10 advisories mais recentes pra cada package que apareceu
#    Filtra por affects=<package> via gh api.
for ecosystem_pkg in "${!SEEN_PACKAGES[@]}"; do
  ecosystem="${ecosystem_pkg%%:*}"
  pkg="${ecosystem_pkg##*:}"
  [[ -z "$pkg" ]] && continue

  gh api "/advisories?ecosystem=$ecosystem&affects=$pkg&per_page=10&sort=published&direction=desc" 2>/dev/null | \
    jq -r --arg ts "$NOW" --arg src "ghsa:$ecosystem:$pkg" '
      (. // [])[] | [
        $ts,
        "advisory",
        (.severity // "unknown"),
        (.cve_id // ""),
        (.ghsa_id // ""),
        $src,
        $src | split(":")[2],
        "",
        "",
        (.summary // ""),
        (.html_url // ""),
        "open"
      ] | @csv' || continue
done
```

Marca executável:
```bash
chmod +x scripts/scan-dependabot.sh
```

- [ ] **Step 2: `push-vulnerabilities.sh` (variação do push-health-results)**

Cria `scripts/push-vulnerabilities.sh`:

```bash
#!/usr/bin/env bash
# Pipeia CSV de vulnerabilidades pro Postgres da VM via SSH dedicada.
# Reusa a mesma SSH key da Sprint 2 (falcao-monitor-push) — command-restricted
# ao psql do monitor_writer, então qualquer stdin via SSH cai no psql.
#
# Uso: ./scan-dependabot.sh | ./push-vulnerabilities.sh
#       ./push-vulnerabilities.sh results.csv

set -euo pipefail

CSV="${1:-/dev/stdin}"
SSH_KEY="${MONITOR_PUSH_SSH_KEY_PATH:-$HOME/.ssh/falcao-monitor-push}"
SSH_HOST="${MONITOR_PUSH_HOST:-162.55.217.189}"
SSH_USER="${MONITOR_PUSH_USER:-falcao}"
KNOWN_HOSTS="${MONITOR_PUSH_KNOWN_HOSTS:-}"

if [[ -n "$KNOWN_HOSTS" && -f "$KNOWN_HOSTS" ]]; then
  SSH_OPTS=(-o "UserKnownHostsFile=$KNOWN_HOSTS" -o "StrictHostKeyChecking=yes")
else
  SSH_OPTS=(-o "StrictHostKeyChecking=accept-new")
fi

# Concatena: \COPY command + CSV body + terminator \.
{
  echo "\\COPY vulnerabilities(ts,kind,severity,cve_id,ghsa_id,source_id,package_name,package_version,fix_version,title,url,state) FROM STDIN WITH (FORMAT csv, HEADER true)"
  cat "$CSV"
  echo "\\."
} | ssh -i "$SSH_KEY" "${SSH_OPTS[@]}" "$SSH_USER@$SSH_HOST"
```

```bash
chmod +x scripts/push-vulnerabilities.sh
```

- [ ] **Step 3: Workflow `.github/workflows/security-scan.yml`**

```yaml
name: Security scan (Dependabot + GHSA cross-cutting)

on:
  schedule:
    # Diariamente às 06:00 UTC (03:00 Brasil). Fora dos minutos de pico.
    - cron: "7 6 * * *"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: security-scan
  cancel-in-progress: false

jobs:
  scan:
    name: Scan Dependabot + GHSA + push to VM
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run scan
        env:
          GH_TOKEN: ${{ secrets.GH_PAT_SECURITY }}
        run: |
          ./scripts/scan-dependabot.sh > /tmp/vulns.csv
          echo "=== Lines collected (incluindo header) ==="
          wc -l /tmp/vulns.csv
          echo "=== Sample ==="
          head -5 /tmp/vulns.csv

      - name: Setup SSH (key + pinned host fingerprint)
        env:
          SSH_KEY: ${{ secrets.MONITOR_PUSH_SSH_KEY }}
          HOST_FINGERPRINT: ${{ secrets.MONITOR_PUSH_HOST_FINGERPRINT }}
        run: |
          mkdir -p ~/.ssh
          printf '%s\n' "$SSH_KEY" > ~/.ssh/monitor_push
          chmod 600 ~/.ssh/monitor_push
          printf '%s\n' "$HOST_FINGERPRINT" > ~/.ssh/known_hosts
          chmod 600 ~/.ssh/known_hosts

      - name: Push vulnerabilities to VM
        env:
          MONITOR_PUSH_SSH_KEY_PATH: /home/runner/.ssh/monitor_push
          MONITOR_PUSH_KNOWN_HOSTS: /home/runner/.ssh/known_hosts
        run: |
          ./scripts/push-vulnerabilities.sh /tmp/vulns.csv
          echo "=== Push concluído ==="
```

> **Nota:** secrets `MONITOR_PUSH_SSH_KEY` e `MONITOR_PUSH_HOST_FINGERPRINT` JÁ existem (Sprint 2 health checks). Só precisa adicionar `GH_PAT_SECURITY` (próxima task).

- [ ] **Step 4: Falcão cria/atualiza GitHub PAT**

> **Manual — você executa:**
>
> 1. Vai em `https://github.com/settings/tokens?type=beta` (Personal Access Tokens fine-grained)
> 2. Click **Generate new token**:
>    - Token name: `falcao-launcher-security-scan`
>    - Expiration: 1 year
>    - Resource owner: `Falkzera`
>    - Repository access: **All repositories**
>    - Permissions:
>      - **Repository permissions:**
>        - `Contents`: Read-only
>        - `Dependabot alerts`: Read-only
>        - `Metadata`: Read-only
>        - `Actions`: Read and write (pra workflow_dispatch)
> 3. Generate. Copia o token (começa com `github_pat_...`).
>
> Adiciona em 2 lugares:
>
> **GitHub Actions secrets** (https://github.com/Falkzera/falcao-launcher/settings/secrets/actions):
> - Name: `GH_PAT_SECURITY`
> - Value: `<token>`
>
> **VM `.env`** (pra Tauri command dispatchar workflow):
> ```bash
> read -s "?Cola o GitHub PAT: " GH_PAT
> ssh falcao@162.55.217.189 "umask 077 && echo 'GITHUB_PAT_SECURITY=$GH_PAT' >> ~/.config/falcao-monitor/.env && chmod 600 ~/.config/falcao-monitor/.env"
> unset GH_PAT
> ```

- [ ] **Step 5: Smoke test local do scan**

```bash
# Local (precisa gh auth login com mesmo PAT, ou GH_TOKEN env var)
GH_TOKEN="$(cat <<<"$GH_PAT_SECURITY")" ./scripts/scan-dependabot.sh > /tmp/vulns-local.csv
wc -l /tmp/vulns-local.csv
head -5 /tmp/vulns-local.csv
```

Expected: ≥ 1 linha (header) + N linhas de CVEs (pode ser zero se não tem alerts ativos, OK).

- [ ] **Step 6: Trigger workflow manual + valida**

```bash
gh workflow run security-scan.yml
# Aguarda ~30s
gh run list --workflow=security-scan.yml --limit 3
gh run watch
```

Expected: workflow conclui ✅. Logs mostram `=== Push concluído ===`.

Validar no DB:
```bash
ssh falcao@162.55.217.189 "docker exec falcao-monitor-db psql -U postgres -d falcao_monitor -c \"SELECT kind, severity, count(*) FROM vulnerabilities GROUP BY kind, severity\""
```

Expected: rows agrupando por kind/severity (ou tabela vazia se não tem alerts).

- [ ] **Step 7: Commit**

```bash
git add scripts/scan-dependabot.sh scripts/push-vulnerabilities.sh .github/workflows/security-scan.yml
git commit -m "feat(security): scan-dependabot.sh + workflow GH Actions cron diário

- scan-dependabot.sh: gh api /user/repos filtra Falkzera/*; pra cada
  repo: dependabot alerts (tolera 404) + GHSA cross-cutting top 10 por
  package.
- push-vulnerabilities.sh: pipe CSV → SSH → \COPY (reusa SSH key
  command-restricted da Sprint 2 health checks).
- Workflow security-scan.yml: cron 0 7 6 * * * (06:07 UTC) +
  workflow_dispatch. Reusa secrets MONITOR_PUSH_SSH_KEY +
  HOST_FINGERPRINT. Novo: GH_PAT_SECURITY (Dependabot read + Actions write)."
```

### Task A3: Trivy scanner na VM (manual + scripts)

**Files (na VM):**
- Create: `/home/falcao/.local/bin/scan-trivy.sh`
- Create: `~/.config/systemd/user/falcao-trivy-scanner.service`
- Create: `~/.config/systemd/user/falcao-trivy-scanner.timer`

- [ ] **Step 1: Falcão instala Trivy na VM**

> **Manual — você executa:**
>
> ```bash
> ssh falcao@162.55.217.189 "curl -sfL https://raw.githubusercontent.com/aquasecurity/trivy/main/contrib/install.sh | sudo sh -s -- -b /usr/local/bin"
> ssh falcao@162.55.217.189 "trivy --version"
> ```
>
> Expected: `Version: 0.x.x`. Primeira execução baixa DB (~60 MB).

- [ ] **Step 2: Garantir que `monitor_writer` pode \\COPY na tabela**

Já feito pelo migration A1 (`GRANT INSERT, SELECT ON vulnerabilities TO monitor_writer`). Validar:

```bash
ssh falcao@162.55.217.189 "docker exec falcao-monitor-db psql -U postgres -d falcao_monitor -c \"\\dp vulnerabilities\""
```

Expected: `monitor_writer=ar/postgres` listado.

- [ ] **Step 3: Setup credentials de DB pra script local na VM**

`scan-trivy.sh` usa `\COPY` via psql local na VM. Precisa de password do `monitor_writer`. Já existe em `/opt/falcao-monitor/.env` (criado na Sprint 1). Validar:

```bash
ssh falcao@162.55.217.189 "grep MONITOR_WRITER_PASSWORD /opt/falcao-monitor/.env | head -1 | sed 's/=.*/=***hidden***/'"
```

Expected: `MONITOR_WRITER_PASSWORD=***hidden***` (chave existe; valor não vazado).

- [ ] **Step 4: Criar `/home/falcao/.local/bin/scan-trivy.sh` na VM**

```bash
ssh falcao@162.55.217.189 "mkdir -p ~/.local/bin"

ssh falcao@162.55.217.189 "cat > ~/.local/bin/scan-trivy.sh" <<'BASH'
#!/usr/bin/env bash
# Roda trivy scan em todas imagens Docker ativas na VM.
# Output direto pro Postgres local via \COPY.
# Sprint B1 — Snyk-like.

set -euo pipefail

# Carrega senha do monitor_writer (e outras envs do monitor)
if [[ -f /opt/falcao-monitor/.env ]]; then
  set -a
  # shellcheck disable=SC1091
  source /opt/falcao-monitor/.env
  set +a
fi

NOW=$(date -u +"%Y-%m-%dT%H:%M:%SZ")
IMAGES=$(docker ps --format '{{.Image}}' | sort -u)

if [[ -z "$IMAGES" ]]; then
  echo "[scan-trivy] nenhuma imagem ativa; nada a fazer" >&2
  exit 0
fi

CSV=$(mktemp)
trap 'rm -f "$CSV"' EXIT

# Header
echo "ts,kind,severity,cve_id,ghsa_id,source_id,package_name,package_version,fix_version,title,url,state" > "$CSV"

for img in $IMAGES; do
  echo "[scan-trivy] scanning $img" >&2
  trivy image \
    --quiet \
    --format json \
    --severity CRITICAL,HIGH,MEDIUM,LOW \
    --no-progress \
    "$img" 2>/dev/null | \
    jq -r --arg ts "$NOW" --arg src "$img" '
      (.Results // [])[] |
      (.Vulnerabilities // [])[] |
      [
        $ts,
        "image",
        (.Severity // "UNKNOWN" | ascii_downcase),
        (.VulnerabilityID // ""),
        "",
        $src,
        (.PkgName // ""),
        (.InstalledVersion // ""),
        (.FixedVersion // ""),
        (.Title // .Description // ""),
        (.PrimaryURL // ""),
        "open"
      ] | @csv' >> "$CSV" || {
      echo "[scan-trivy] skip $img: trivy or jq failed" >&2
    }
done

LINES=$(wc -l < "$CSV")
echo "[scan-trivy] $((LINES - 1)) findings collected" >&2

# Push pro Postgres local via psql + \COPY
PGPASSWORD="$MONITOR_WRITER_PASSWORD" docker exec -i falcao-monitor-db psql \
  -U monitor_writer \
  -d falcao_monitor \
  -h localhost \
  -c "\\COPY vulnerabilities(ts,kind,severity,cve_id,ghsa_id,source_id,package_name,package_version,fix_version,title,url,state) FROM STDIN WITH (FORMAT csv, HEADER true)" \
  < "$CSV"

echo "[scan-trivy] pushed to DB" >&2
BASH

ssh falcao@162.55.217.189 "chmod +x ~/.local/bin/scan-trivy.sh && cat ~/.local/bin/scan-trivy.sh | head -10"
```

Expected: arquivo criado, executável.

- [ ] **Step 5: Smoke test do scan-trivy**

```bash
ssh falcao@162.55.217.189 "~/.local/bin/scan-trivy.sh 2>&1" | tail -10
```

Expected:
- `[scan-trivy] scanning caddy:2-alpine`
- `[scan-trivy] scanning timescale/timescaledb-ha:pg16`
- `[scan-trivy] scanning falcao-financas-app`
- `[scan-trivy] N findings collected`
- `[scan-trivy] pushed to DB`

(Primeira execução demora 2-5 min — Trivy baixa DB.)

Valida no DB:
```bash
ssh falcao@162.55.217.189 "docker exec falcao-monitor-db psql -U postgres -d falcao_monitor -c \"SELECT kind, severity, count(*) FROM vulnerabilities WHERE kind='image' GROUP BY kind, severity ORDER BY severity\""
```

Expected: rows com `image` + counts por severidade.

- [ ] **Step 6: systemd timer pra rodar diariamente**

```bash
ssh falcao@162.55.217.189 "cat > ~/.config/systemd/user/falcao-trivy-scanner.service" <<'INI'
[Unit]
Description=Trivy vulnerability scanner pra imagens Docker ativas
After=docker.service

[Service]
Type=oneshot
ExecStart=/home/falcao/.local/bin/scan-trivy.sh
StandardOutput=journal
StandardError=journal
INI

ssh falcao@162.55.217.189 "cat > ~/.config/systemd/user/falcao-trivy-scanner.timer" <<'INI'
[Unit]
Description=Run trivy scanner daily

[Timer]
OnCalendar=daily
RandomizedDelaySec=10min
Persistent=true

[Install]
WantedBy=timers.target
INI

ssh falcao@162.55.217.189 "systemctl --user daemon-reload && systemctl --user enable --now falcao-trivy-scanner.timer && systemctl --user list-timers --all | grep falcao-trivy"
```

Expected: `falcao-trivy-scanner.timer` aparece com `NEXT` no próximo dia 00:00 UTC + delay aleatório.

- [ ] **Step 7: Commit (apenas docs do trivy setup — scripts ficam só na VM por hora)**

> **Nota:** o `scan-trivy.sh` mora na VM (não no repo). Vai pro repo na Phase E como referência em `docs/superpowers/`. Esse step só commita o que está no repo (nada novo nessa task — tudo é VM-side).

Pula commit. Próxima task.

---

## Phase B — Backend Rust

> **Paralelizável com Phase C** após C1 (tipos TS) — mas C1 é trivial e B1 deve sair primeiro pra UI consumir tipos consistentes. Recomendo executar B1 → (B2 ‖ B3 ‖ B4 ‖ Phase C) — ou tudo sequencial se preferir simplicidade.

### Task B1: `src-tauri/src/monitor/security.rs`

**Files:**
- Create: `src-tauri/src/monitor/security.rs`
- Modify: `src-tauri/src/monitor/mod.rs`

- [ ] **Step 1: Criar arquivo com tipos + queries**

Cria `src-tauri/src/monitor/security.rs`:

```rust
//! Queries SELECT pra tabela `vulnerabilities` (Sprint B1 — Snyk-like).
//!
//! Tabela única heterogênea com kind discriminator (deps|image|advisory).
//! Filtragem por severidade + kind no SQL (mais barato que client-side).

use anyhow::{Context, Result};
use chrono::{DateTime, Utc};
use deadpool_postgres::Pool;
use serde::Serialize;
use std::collections::HashMap;

#[derive(Debug, Clone, Serialize)]
pub struct VulnerabilityRow {
    pub kind: String,
    pub severity: String,
    pub cve_id: Option<String>,
    pub ghsa_id: Option<String>,
    pub source_id: String,
    pub package_name: Option<String>,
    pub package_version: Option<String>,
    pub fix_version: Option<String>,
    pub title: Option<String>,
    pub url: Option<String>,
    pub state: String,
    pub last_seen: DateTime<Utc>,
}

#[derive(Debug, Serialize)]
pub struct VulnSummary {
    pub critical: i64,
    pub high: i64,
    pub medium: i64,
    pub low: i64,
    pub last_scan: Option<DateTime<Utc>>,
}

/// Lista vulnerabilidades open com filtros.
/// Retorna apenas a observação MAIS RECENTE de cada (kind, source_id, cve_id) —
/// evita duplicados quando scans repetidos persistem o mesmo CVE.
pub async fn list_vulnerabilities(
    pool: &Pool,
    severities: &[String],
    kinds: &[String],
) -> Result<Vec<VulnerabilityRow>> {
    let client = pool.get().await?;

    // Whitelist defensiva — evita SQL injection mesmo com prepared.
    let valid_severities = ["critical", "high", "medium", "low", "unknown"];
    let valid_kinds = ["deps", "image", "advisory"];

    let sev_filter: Vec<String> = severities
        .iter()
        .filter(|s| valid_severities.contains(&s.as_str()))
        .cloned()
        .collect();
    let kind_filter: Vec<String> = kinds
        .iter()
        .filter(|k| valid_kinds.contains(&k.as_str()))
        .cloned()
        .collect();

    if sev_filter.is_empty() || kind_filter.is_empty() {
        return Ok(Vec::new());
    }

    // DISTINCT ON pega o registro mais recente por (kind, source_id, cve_id|ghsa_id).
    // ts > now() - 7d garante que vulnerabilidades obsoletas (não vistas em scans
    // recentes) não aparecem mesmo que estejam na hypertable de 90d.
    let rows = client
        .query(
            r#"
            SELECT DISTINCT ON (kind, source_id, COALESCE(cve_id, ghsa_id, package_name))
              kind, severity, cve_id, ghsa_id, source_id, package_name,
              package_version, fix_version, title, url, state, ts
            FROM vulnerabilities
            WHERE state = 'open'
              AND ts > now() - interval '7 days'
              AND severity = ANY($1)
              AND kind = ANY($2)
            ORDER BY kind, source_id, COALESCE(cve_id, ghsa_id, package_name), ts DESC
            "#,
            &[&sev_filter, &kind_filter],
        )
        .await
        .context("list vulnerabilities")?;

    Ok(rows
        .into_iter()
        .map(|r| VulnerabilityRow {
            kind: r.get(0),
            severity: r.get(1),
            cve_id: r.get(2),
            ghsa_id: r.get(3),
            source_id: r.get(4),
            package_name: r.get(5),
            package_version: r.get(6),
            fix_version: r.get(7),
            title: r.get(8),
            url: r.get(9),
            state: r.get(10),
            last_seen: r.get(11),
        })
        .collect())
}

/// Contadores por severidade (apenas open + vistos em últimos 7d).
pub async fn vuln_summary(pool: &Pool) -> Result<VulnSummary> {
    let client = pool.get().await?;

    let row = client
        .query_one(
            r#"
            WITH latest AS (
              SELECT DISTINCT ON (kind, source_id, COALESCE(cve_id, ghsa_id, package_name))
                severity, ts
              FROM vulnerabilities
              WHERE state = 'open' AND ts > now() - interval '7 days'
              ORDER BY kind, source_id, COALESCE(cve_id, ghsa_id, package_name), ts DESC
            )
            SELECT
              COUNT(*) FILTER (WHERE severity = 'critical') AS critical,
              COUNT(*) FILTER (WHERE severity = 'high')     AS high,
              COUNT(*) FILTER (WHERE severity = 'medium')   AS medium,
              COUNT(*) FILTER (WHERE severity = 'low')      AS low,
              MAX(ts) AS last_scan
            FROM latest
            "#,
            &[],
        )
        .await
        .context("vuln summary")?;

    Ok(VulnSummary {
        critical: row.get(0),
        high: row.get(1),
        medium: row.get(2),
        low: row.get(3),
        last_scan: row.get(4),
    })
}

/// Contagem de Critical+High open por repo (kind=deps), pros chips de ProjectCard.
/// Retorna `{ "Falkzera/repo": 3, ... }`.
pub async fn vuln_count_by_repo(pool: &Pool) -> Result<HashMap<String, i64>> {
    let client = pool.get().await?;

    let rows = client
        .query(
            r#"
            WITH latest AS (
              SELECT DISTINCT ON (kind, source_id, COALESCE(cve_id, ghsa_id, package_name))
                kind, source_id, severity
              FROM vulnerabilities
              WHERE state = 'open' AND ts > now() - interval '7 days'
              ORDER BY kind, source_id, COALESCE(cve_id, ghsa_id, package_name), ts DESC
            )
            SELECT source_id, COUNT(*)
            FROM latest
            WHERE kind = 'deps' AND severity IN ('critical', 'high')
            GROUP BY source_id
            "#,
            &[],
        )
        .await
        .context("vuln count by repo")?;

    let mut out = HashMap::with_capacity(rows.len());
    for r in rows {
        out.insert(r.get::<_, String>(0), r.get::<_, i64>(1));
    }
    Ok(out)
}
```

- [ ] **Step 2: Re-export em `mod.rs`**

```bash
grep -n "pub mod" src-tauri/src/monitor/mod.rs
```

Edita `src-tauri/src/monitor/mod.rs`. Adiciona:
```rust
pub mod security;
```

(Em ordem alfabética — depois de `queries`, antes de `stacks` ou similar.)

- [ ] **Step 3: Build smoke**

```bash
cargo build --manifest-path src-tauri/Cargo.toml 2>&1 | tail -5
```

Expected: build limpo, sem warnings novos.

- [ ] **Step 4: Commit**

```bash
git add src-tauri/src/monitor/security.rs src-tauri/src/monitor/mod.rs
git commit -m "feat(security): security.rs com queries pra tabela vulnerabilities

- VulnerabilityRow + VulnSummary tipos serializáveis
- list_vulnerabilities(pool, severities, kinds): DISTINCT ON pra dedup,
  whitelist defensiva de severities/kinds, filtro state=open + ts>7d
- vuln_summary(pool): contadores por severidade + last_scan
- vuln_count_by_repo(pool): { repo: count } pra chips Critical/High dos
  ProjectCards (apenas kind=deps)"
```

### Task B2: Comandos Tauri novos em `commands.rs`

**Files:**
- Modify: `src-tauri/src/monitor/commands.rs`

- [ ] **Step 1: Adicionar comandos no fim do arquivo**

Edita `src-tauri/src/monitor/commands.rs`. No topo, adicionar import:

```rust
use crate::monitor::security::{self, VulnSummary, VulnerabilityRow};
use std::collections::HashMap;
```

No fim do arquivo, adicionar 3 comandos:

```rust
/// Lista vulnerabilidades filtradas por severidade + kind (Sprint B1).
#[tauri::command]
pub async fn monitor_list_vulnerabilities(
    severities: Vec<String>,
    kinds: Vec<String>,
    state: State<'_, MonitorState>,
) -> Result<Vec<VulnerabilityRow>, String> {
    let pool = state
        .pool
        .lock()
        .unwrap()
        .clone()
        .ok_or_else(|| "tunnel closed".to_string())?;
    security::list_vulnerabilities(&pool, &severities, &kinds)
        .await
        .map_err(|e| e.to_string())
}

/// Contadores de vulnerabilidades por severidade (Sprint B1).
#[tauri::command]
pub async fn monitor_vuln_summary(
    state: State<'_, MonitorState>,
) -> Result<VulnSummary, String> {
    let pool = state
        .pool
        .lock()
        .unwrap()
        .clone()
        .ok_or_else(|| "tunnel closed".to_string())?;
    security::vuln_summary(&pool).await.map_err(|e| e.to_string())
}

/// Contagem de Critical+High por repo, pros chips dos ProjectCards (Sprint B1).
#[tauri::command]
pub async fn monitor_vuln_count_by_repo(
    state: State<'_, MonitorState>,
) -> Result<HashMap<String, i64>, String> {
    let pool = state
        .pool
        .lock()
        .unwrap()
        .clone()
        .ok_or_else(|| "tunnel closed".to_string())?;
    security::vuln_count_by_repo(&pool)
        .await
        .map_err(|e| e.to_string())
}
```

- [ ] **Step 2: Registrar em `lib.rs`**

```bash
grep -n "monitor_stack_detail\|generate_handler" src-tauri/src/lib.rs | head -5
```

Edita `src-tauri/src/lib.rs`. Adiciona ao `tauri::generate_handler!`:
```
monitor::commands::monitor_list_vulnerabilities,
monitor::commands::monitor_vuln_summary,
monitor::commands::monitor_vuln_count_by_repo,
```

(logo após `monitor::commands::monitor_stack_detail,` ou similar).

- [ ] **Step 3: Build smoke**

```bash
cargo build --manifest-path src-tauri/Cargo.toml 2>&1 | tail -5
```

Expected: limpo.

- [ ] **Step 4: Commit**

```bash
git add src-tauri/src/monitor/commands.rs src-tauri/src/lib.rs
git commit -m "feat(security): comandos Tauri pra vulnerabilidades

- monitor_list_vulnerabilities(severities, kinds): lista filtrada
- monitor_vuln_summary(): contadores por severidade
- monitor_vuln_count_by_repo(): chips dos ProjectCards (kind=deps)
- Registrados no tauri::generate_handler"
```

### Task B3: `external.rs` — trigger scan on-demand

**Files:**
- Modify: `src-tauri/src/external.rs`

- [ ] **Step 1: Adicionar trigger pra Trivy via SSH**

Edita `src-tauri/src/external.rs`. No fim do arquivo (mas antes do `mod tests` se houver):

```rust
/// Dispara `scan-trivy.sh` na VM e emite eventos `vuln-scan-progress`
/// pra UI mostrar streaming. Sprint B1 — Snyk-like.
#[tauri::command]
pub async fn trigger_trivy_scan_on_vm(
    app: tauri::AppHandle,
) -> Result<(), String> {
    use tauri::Emitter;
    use tokio::io::{AsyncBufReadExt, BufReader};

    let mut child = tokio::process::Command::new("ssh")
        .args(["falcao@162.55.217.189", "/home/falcao/.local/bin/scan-trivy.sh"])
        .stdout(std::process::Stdio::piped())
        .stderr(std::process::Stdio::piped())
        .spawn()
        .map_err(|e| format!("falha spawn ssh: {}", e))?;

    let stderr = child
        .stderr
        .take()
        .ok_or_else(|| "stderr unavailable".to_string())?;

    // scan-trivy.sh emite progresso em stderr (`[scan-trivy] scanning ...`).
    // Tail stderr em task paralela emitindo eventos.
    let app_clone = app.clone();
    tokio::spawn(async move {
        let mut reader = BufReader::new(stderr).lines();
        while let Ok(Some(line)) = reader.next_line().await {
            let _ = app_clone.emit(
                "vuln-scan-progress",
                serde_json::json!({ "kind": "image", "line": line }),
            );
        }
    });

    let status = child.wait().await.map_err(|e| e.to_string())?;
    if !status.success() {
        return Err(format!("scan-trivy.sh exited with {}", status));
    }
    Ok(())
}

/// Dispara o workflow `security-scan.yml` no GitHub via PAT.
/// Sprint B1 — Snyk-like.
#[tauri::command]
pub async fn trigger_dependabot_scan_via_gh() -> Result<(), String> {
    use std::process::Stdio;

    // Executa `gh workflow run security-scan.yml` localmente
    // (gh CLI já está configurada com auth do Falcão).
    let mut cmd = tokio::process::Command::new("gh");
    cmd.args([
        "workflow",
        "run",
        "security-scan.yml",
        "-R",
        "Falkzera/falcao-launcher",
    ])
    .stdout(Stdio::null())
    .stderr(Stdio::piped());

    // PATH com ~/.local/bin (gh pode estar lá em algumas distros)
    if let Some(home) = dirs::home_dir() {
        let local_bin = home.join(".local").join("bin");
        let current_path = std::env::var("PATH")
            .unwrap_or_else(|_| "/usr/local/bin:/usr/bin:/bin".into());
        cmd.env("PATH", format!("{}:{}", local_bin.display(), current_path));
    }

    let output = cmd.output().await.map_err(|e| e.to_string())?;
    if !output.status.success() {
        let err = String::from_utf8_lossy(&output.stderr);
        return Err(format!("gh workflow run falhou: {}", err));
    }
    Ok(())
}
```

- [ ] **Step 2: Registrar comandos em `lib.rs`**

```bash
grep -n "spawn_claude_investigation" src-tauri/src/lib.rs
```

Edita `src-tauri/src/lib.rs`. Adiciona ao `tauri::generate_handler!`:
```
external::trigger_trivy_scan_on_vm,
external::trigger_dependabot_scan_via_gh,
```

- [ ] **Step 3: Build smoke**

```bash
cargo build --manifest-path src-tauri/Cargo.toml 2>&1 | tail -5
```

- [ ] **Step 4: Commit**

```bash
git add src-tauri/src/external.rs src-tauri/src/lib.rs
git commit -m "feat(security): trigger_trivy_scan_on_vm + trigger_dependabot_scan_via_gh

- trigger_trivy_scan_on_vm: SSH na VM rodando scan-trivy.sh, tail stderr
  emite eventos 'vuln-scan-progress' pra UI mostrar streaming.
- trigger_dependabot_scan_via_gh: gh workflow run security-scan.yml -R
  Falkzera/falcao-launcher (usa gh CLI local com auth do Falcão)."
```

### Task B4: `config.rs` — `dismissed_vulnerabilities`

**Files:**
- Modify: `src-tauri/src/config.rs`

- [ ] **Step 1: Adicionar struct + field + comandos**

Edita `src-tauri/src/config.rs`. Adiciona depois das structs existentes:

```rust
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
pub struct DismissedVuln {
    pub fix_version_at_time: Option<String>,
    pub dismissed_at: chrono::DateTime<chrono::Utc>,
}
```

No struct `Config`, adiciona campo (depois dos existentes):

```rust
#[serde(default)]
pub dismissed_vulnerabilities: HashMap<String, DismissedVuln>,
```

(Garante que `use std::collections::HashMap` está nos imports.)

Adiciona 2 comandos novos no fim do arquivo:

```rust
/// Marca CVE como dismissed (false positive). Key formato "<source_id>:<cve_id>".
/// Sprint B1 — Snyk-like.
#[tauri::command]
pub fn dismiss_cve(
    cve_key: String,
    fix_version_at_time: Option<String>,
) -> Result<(), String> {
    let mut config = load_config().map_err(|e| e.to_string())?;
    config.dismissed_vulnerabilities.insert(
        cve_key,
        DismissedVuln {
            fix_version_at_time,
            dismissed_at: chrono::Utc::now(),
        },
    );
    save_config(&config).map_err(|e| e.to_string())?;
    Ok(())
}

#[tauri::command]
pub fn undismiss_cve(cve_key: String) -> Result<(), String> {
    let mut config = load_config().map_err(|e| e.to_string())?;
    config.dismissed_vulnerabilities.remove(&cve_key);
    save_config(&config).map_err(|e| e.to_string())?;
    Ok(())
}

/// Retorna todos os CVEs dismissed (chave + fix_version_at_time).
/// Frontend usa pra cruzar com lista vinda do DB e ocultar/revalidar.
#[tauri::command]
pub fn list_dismissed_cves() -> Result<HashMap<String, DismissedVuln>, String> {
    let config = load_config().map_err(|e| e.to_string())?;
    Ok(config.dismissed_vulnerabilities)
}
```

- [ ] **Step 2: Registrar em `lib.rs`**

```bash
grep -n "set_project_config\|generate_handler" src-tauri/src/lib.rs | head -3
```

Adiciona ao `tauri::generate_handler!`:
```
config::dismiss_cve,
config::undismiss_cve,
config::list_dismissed_cves,
```

- [ ] **Step 3: Build smoke**

```bash
cargo build --manifest-path src-tauri/Cargo.toml 2>&1 | tail -5
```

- [ ] **Step 4: Commit**

```bash
git add src-tauri/src/config.rs src-tauri/src/lib.rs
git commit -m "feat(security): dismissed_vulnerabilities em ~/.config/falcao-launcher/config.json

- DismissedVuln { fix_version_at_time, dismissed_at }
- Config.dismissed_vulnerabilities: HashMap<String, DismissedVuln>
  (key formato 'source_id:cve_id')
- Comandos dismiss_cve(key, fix), undismiss_cve(key), list_dismissed_cves()
- Frontend cruza com lista do DB pra ocultar e revalidar quando
  fix_version mudar."
```

---

## Phase C — Frontend

### Task C1: Tipos TS + monitorApi wrappers

**Files:**
- Create: `src/types/security.ts`
- Modify: `src/lib/monitor.ts`

- [ ] **Step 1: Criar `src/types/security.ts`**

```typescript
// Tipos do "Snyk-like" (Sprint B1). Espelham structs Rust em
// src-tauri/src/monitor/security.rs e src-tauri/src/config.rs.

export type VulnSeverity = "critical" | "high" | "medium" | "low" | "unknown";
export type VulnKind = "deps" | "image" | "advisory";
export type VulnState = "open" | "fixed" | "dismissed";

export interface VulnerabilityRow {
  kind: VulnKind;
  severity: VulnSeverity;
  cve_id: string | null;
  ghsa_id: string | null;
  source_id: string;
  package_name: string | null;
  package_version: string | null;
  fix_version: string | null;
  title: string | null;
  url: string | null;
  state: VulnState;
  last_seen: string; // ISO
}

export interface VulnSummary {
  critical: number;
  high: number;
  medium: number;
  low: number;
  last_scan: string | null;
}

export interface DismissedVuln {
  fix_version_at_time: string | null;
  dismissed_at: string;
}

export interface VulnFilters {
  severities: VulnSeverity[];
  kinds: VulnKind[];
  search: string;
}

/** Constrói chave canônica pra dismiss (formato Rust: "source_id:cve_id"). */
export function vulnDismissKey(vuln: VulnerabilityRow): string {
  const id = vuln.cve_id ?? vuln.ghsa_id ?? vuln.package_name ?? "unknown";
  return `${vuln.source_id}:${id}`;
}

/** Verifica se um CVE dismissed deve ser revalidado (fix_version mudou). */
export function shouldRevalidateDismiss(
  vuln: VulnerabilityRow,
  dismissed: DismissedVuln,
): boolean {
  return (vuln.fix_version ?? null) !== (dismissed.fix_version_at_time ?? null);
}
```

- [ ] **Step 2: Adicionar wrappers ao monitorApi**

Edita `src/lib/monitor.ts`. Adiciona imports no topo:

```typescript
import type {
  DismissedVuln,
  VulnFilters,
  VulnSummary,
  VulnerabilityRow,
} from "../types/security";
```

Adiciona método ao `monitorApi`:

```typescript
  // Sprint B1 — Snyk-like
  listVulnerabilities: (filters: VulnFilters) =>
    invoke<VulnerabilityRow[]>("monitor_list_vulnerabilities", {
      severities: filters.severities,
      kinds: filters.kinds,
    }),
  vulnSummary: () => invoke<VulnSummary>("monitor_vuln_summary"),
  vulnCountByRepo: () =>
    invoke<Record<string, number>>("monitor_vuln_count_by_repo"),
  triggerTrivyScan: () => invoke<void>("trigger_trivy_scan_on_vm"),
  triggerDependabotScan: () =>
    invoke<void>("trigger_dependabot_scan_via_gh"),
  dismissCve: (cveKey: string, fixVersionAtTime: string | null) =>
    invoke<void>("dismiss_cve", { cveKey, fixVersionAtTime }),
  undismissCve: (cveKey: string) =>
    invoke<void>("undismiss_cve", { cveKey }),
  listDismissedCves: () =>
    invoke<Record<string, DismissedVuln>>("list_dismissed_cves"),
```

- [ ] **Step 3: Type-check**

```bash
pnpm exec tsc --noEmit 2>&1 | tail -5
```

Expected: zero erros.

- [ ] **Step 4: Commit**

```bash
git add src/types/security.ts src/lib/monitor.ts
git commit -m "feat(security): tipos TS + 8 wrappers monitorApi pra vulnerabilidades

- Types: VulnSeverity, VulnKind, VulnerabilityRow, VulnSummary,
  DismissedVuln, VulnFilters
- Helpers: vulnDismissKey() e shouldRevalidateDismiss()
- monitorApi: listVulnerabilities, vulnSummary, vulnCountByRepo,
  triggerTrivyScan, triggerDependabotScan, dismissCve, undismissCve,
  listDismissedCves"
```

### Task C2: `SecurityChip.tsx` (componente compacto)

**Files:**
- Create: `src/components/SecurityChip.tsx`

- [ ] **Step 1: Criar componente**

```tsx
interface Props {
  count: number;
}

/**
 * Chip compacto pra ProjectCard quando há CVE Critical/High open
 * & não-dismissado. Render `null` se count == 0 (sem ruído visual).
 *
 * Sprint B1 — Snyk-like.
 */
export function SecurityChip({ count }: Props) {
  if (count <= 0) return null;
  return (
    <span
      className="rounded-full border border-[var(--color-danger)]/40 bg-[var(--color-danger-soft)] px-1.5 py-0.5 font-mono text-[9px] font-semibold uppercase tracking-wider text-[var(--color-danger)]"
      title={`${count} CVE${count === 1 ? "" : "s"} Critical/High aberto${count === 1 ? "" : "s"}`}
    >
      ⚠ {count} CVE
    </span>
  );
}
```

- [ ] **Step 2: Type-check**

```bash
pnpm exec tsc --noEmit 2>&1 | tail -3
```

- [ ] **Step 3: Commit**

```bash
git add src/components/SecurityChip.tsx
git commit -m "feat(security): SecurityChip — chip compacto pros ProjectCards

- Render null quando count == 0 (sem ruído)
- Cor danger + título explicativo
- Sprint B1 — Snyk-like"
```

### Task C3: `VulnerabilityRow.tsx`

**Files:**
- Create: `src/components/VulnerabilityRow.tsx`

- [ ] **Step 1: Criar componente**

```tsx
import type { VulnerabilityRow as VulnRow } from "../types/security";

interface Props {
  vuln: VulnRow;
  isDismissed: boolean;
  onDismiss: () => void;
  onUndismiss: () => void;
}

const SEVERITY_COLOR: Record<string, string> = {
  critical: "var(--color-danger)",
  high: "var(--color-warning)",
  medium: "var(--color-accent-secondary)",
  low: "var(--color-text-muted)",
  unknown: "var(--color-text-muted)",
};

/**
 * Linha individual de CVE no SecurityTab (Sprint B1).
 * Severity chip + título + package + fix_version + link + dismiss btn.
 */
export function VulnerabilityRow({
  vuln,
  isDismissed,
  onDismiss,
  onUndismiss,
}: Props) {
  const sevColor = SEVERITY_COLOR[vuln.severity] ?? SEVERITY_COLOR.unknown;
  const id = vuln.cve_id ?? vuln.ghsa_id ?? vuln.package_name ?? "unknown";

  return (
    <div
      className={
        "flex flex-wrap items-center gap-2 rounded-md border border-[var(--color-border-subtle)] bg-[var(--color-bg-card)] p-2 text-xs " +
        (isDismissed ? "opacity-50" : "")
      }
    >
      <span
        className="rounded-full px-1.5 py-0.5 font-mono text-[9px] font-semibold uppercase tracking-wider"
        style={{ background: sevColor, color: "black" }}
      >
        {vuln.severity}
      </span>

      <span className="font-mono text-[10px] text-[var(--color-text-secondary)]">
        {id}
      </span>

      {vuln.package_name && (
        <span className="rounded border border-[var(--color-border-subtle)] bg-[var(--color-bg-glass)] px-1.5 py-0.5 font-mono text-[10px]">
          {vuln.package_name}
          {vuln.package_version && (
            <span className="text-[var(--color-text-muted)]">
              {" "}@{vuln.package_version}
            </span>
          )}
        </span>
      )}

      {vuln.fix_version && (
        <span className="font-mono text-[10px] text-[var(--color-success)]">
          → {vuln.fix_version}
        </span>
      )}

      <span className="min-w-0 flex-1 truncate" title={vuln.title ?? ""}>
        {vuln.title ?? "(sem título)"}
      </span>

      {vuln.url && (
        <a
          href={vuln.url}
          target="_blank"
          rel="noopener noreferrer"
          className="text-[var(--color-accent-primary)] hover:underline"
          onClick={(e) => e.stopPropagation()}
        >
          ↗
        </a>
      )}

      <button
        onClick={isDismissed ? onUndismiss : onDismiss}
        className="rounded border border-[var(--color-border-subtle)] px-1.5 py-0.5 text-[10px] text-[var(--color-text-secondary)] transition hover:border-[var(--color-accent-primary)]/60 hover:text-[var(--color-accent-primary)]"
      >
        {isDismissed ? "Restaurar" : "Dismissar"}
      </button>
    </div>
  );
}
```

- [ ] **Step 2: Type-check**

```bash
pnpm exec tsc --noEmit 2>&1 | tail -3
```

- [ ] **Step 3: Commit**

```bash
git add src/components/VulnerabilityRow.tsx
git commit -m "feat(security): VulnerabilityRow — item individual da lista

- Severity chip colorido por severidade
- ID (CVE_id ou GHSA_id ou package fallback)
- Package + version chip
- Fix version (verde com seta)
- Título truncado
- Link advisory (↗) com stopPropagation
- Botão dismiss/restore
- Opacity 50% quando dismissed"
```

### Task C4: `SecurityScanProgress.tsx`

**Files:**
- Create: `src/components/SecurityScanProgress.tsx`

- [ ] **Step 1: Criar componente**

```tsx
import { useEffect, useState } from "react";
import { listen } from "@tauri-apps/api/event";
import { AnimatePresence, motion } from "framer-motion";
import { InlineLoading } from "./Loading";

interface Props {
  scanning: boolean;
  onClose: () => void;
}

interface ProgressEvent {
  kind: "image" | "deps";
  line: string;
}

/**
 * Overlay durante scan on-demand. Streaming de eventos `vuln-scan-progress`
 * vindos do Rust (SSH stderr tail). Sprint B1 — Snyk-like.
 */
export function SecurityScanProgress({ scanning, onClose }: Props) {
  const [lines, setLines] = useState<string[]>([]);

  useEffect(() => {
    if (!scanning) {
      setLines([]);
      return;
    }

    const unlistenPromise = listen<ProgressEvent>("vuln-scan-progress", (event) => {
      setLines((prev) => [...prev.slice(-49), event.payload.line]);
    });

    return () => {
      unlistenPromise.then((unlisten) => unlisten());
    };
  }, [scanning]);

  return (
    <AnimatePresence>
      {scanning && (
        <motion.div
          initial={{ opacity: 0 }}
          animate={{ opacity: 1 }}
          exit={{ opacity: 0 }}
          className="fixed inset-0 z-40 flex items-center justify-center bg-black/60"
          onClick={onClose}
        >
          <motion.div
            initial={{ scale: 0.95 }}
            animate={{ scale: 1 }}
            exit={{ scale: 0.95 }}
            className="w-[min(560px,90vw)] rounded-2xl border border-[var(--color-border-subtle)] bg-[var(--color-bg-secondary)] p-5 shadow-[0_8px_32px_rgba(0,0,0,0.5)]"
            onClick={(e) => e.stopPropagation()}
          >
            <h3 className="page-title mb-3 text-lg text-[var(--color-text-primary)]">
              🔍 Scan em progresso
            </h3>
            <InlineLoading
              minHeight="3rem"
              messages={[
                "Buscando Dependabot alerts",
                "Trivy escaneando imagens Docker",
                "Cross-checking GHSA advisories",
                "Quase lá",
              ]}
            />
            {lines.length > 0 && (
              <pre className="mt-3 max-h-48 overflow-auto rounded-md border border-[var(--color-border-subtle)] bg-[var(--color-bg-primary)] p-2 font-mono text-[10px] leading-relaxed text-[var(--color-text-muted)]">
                {lines.join("\n")}
              </pre>
            )}
            <div className="mt-3 text-right">
              <button
                onClick={onClose}
                className="rounded-md border border-[var(--color-border-subtle)] px-3 py-1 text-xs text-[var(--color-text-secondary)] hover:text-[var(--color-text-primary)]"
              >
                Fechar (continua em background)
              </button>
            </div>
          </motion.div>
        </motion.div>
      )}
    </AnimatePresence>
  );
}
```

- [ ] **Step 2: Type-check**

```bash
pnpm exec tsc --noEmit 2>&1 | tail -3
```

- [ ] **Step 3: Commit**

```bash
git add src/components/SecurityScanProgress.tsx
git commit -m "feat(security): SecurityScanProgress — overlay com streaming de scan

- Listen no evento Tauri 'vuln-scan-progress' (lines do scan-trivy stderr)
- InlineLoading com 4 mensagens cíclicas
- Pre com últimas 50 linhas do progresso
- Click fora ou botão 'Fechar' não cancela o scan (continua em bg)"
```

### Task C5: `SecurityTab.tsx` (orquestrador)

**Files:**
- Create: `src/components/SecurityTab.tsx`

- [ ] **Step 1: Criar componente**

```tsx
import { useEffect, useMemo, useState } from "react";
import { monitorApi, useTunnel, usePolling } from "../lib/monitor";
import { InlineLoading } from "./Loading";
import { SecurityScanProgress } from "./SecurityScanProgress";
import { VulnerabilityRow } from "./VulnerabilityRow";
import {
  shouldRevalidateDismiss,
  vulnDismissKey,
  type DismissedVuln,
  type VulnFilters,
  type VulnKind,
  type VulnSeverity,
  type VulnerabilityRow as VulnRow,
} from "../types/security";

const DEFAULT_FILTERS: VulnFilters = {
  severities: ["critical", "high"],
  kinds: ["deps", "image", "advisory"],
  search: "",
};

const SEV_LABEL: Record<VulnSeverity, string> = {
  critical: "Critical",
  high: "High",
  medium: "Medium",
  low: "Low",
  unknown: "Unknown",
};

const KIND_LABEL: Record<VulnKind, string> = {
  deps: "deps",
  image: "image",
  advisory: "advisory",
};

export function SecurityTab() {
  const { ready } = useTunnel();
  const [filters, setFilters] = useState<VulnFilters>(DEFAULT_FILTERS);
  const [scanning, setScanning] = useState(false);
  const [dismissed, setDismissed] = useState<Record<string, DismissedVuln>>({});

  // Polling — leve, vulnerabilidades não mudam minuto a minuto
  const { data: vulns, error: vulnsError } = usePolling(
    () => monitorApi.listVulnerabilities(filters),
    60_000,
    ready,
  );
  const { data: summary } = usePolling(monitorApi.vulnSummary, 60_000, ready);

  // Carrega dismiss persistido no boot e após operações
  const refreshDismissed = () => {
    monitorApi
      .listDismissedCves()
      .then(setDismissed)
      .catch((e) => console.warn("listDismissedCves failed:", e));
  };
  useEffect(() => {
    refreshDismissed();
  }, []);

  // Aplica dismiss + revalidação (fix_version mudou)
  const visibleVulns = useMemo(() => {
    if (!vulns) return null;
    return vulns.filter((v) => {
      const key = vulnDismissKey(v);
      const d = dismissed[key];
      if (!d) return true;
      // Revalida se fix_version mudou — reaparece
      return shouldRevalidateDismiss(v, d);
    });
  }, [vulns, dismissed]);

  // Agrupa por source_id pra render
  const grouped = useMemo(() => {
    if (!visibleVulns) return null;
    const map = new Map<string, VulnRow[]>();
    for (const v of visibleVulns) {
      if (!map.has(v.source_id)) map.set(v.source_id, []);
      map.get(v.source_id)!.push(v);
    }
    // Ordena: critical/high primeiro dentro de cada source
    for (const [, list] of map) {
      list.sort((a, b) => severityRank(a.severity) - severityRank(b.severity));
    }
    return Array.from(map.entries()).sort();
  }, [visibleVulns]);

  const handleScan = async () => {
    setScanning(true);
    try {
      // Dispara os 2 em paralelo
      await Promise.allSettled([
        monitorApi.triggerTrivyScan(),
        monitorApi.triggerDependabotScan(),
      ]);
    } finally {
      setScanning(false);
    }
  };

  const toggleSeverity = (sev: VulnSeverity) => {
    setFilters((prev) => ({
      ...prev,
      severities: prev.severities.includes(sev)
        ? prev.severities.filter((s) => s !== sev)
        : [...prev.severities, sev],
    }));
  };

  const toggleKind = (k: VulnKind) => {
    setFilters((prev) => ({
      ...prev,
      kinds: prev.kinds.includes(k)
        ? prev.kinds.filter((x) => x !== k)
        : [...prev.kinds, k],
    }));
  };

  const handleDismiss = async (vuln: VulnRow) => {
    const key = vulnDismissKey(vuln);
    await monitorApi.dismissCve(key, vuln.fix_version);
    refreshDismissed();
  };

  const handleUndismiss = async (vuln: VulnRow) => {
    const key = vulnDismissKey(vuln);
    await monitorApi.undismissCve(key);
    refreshDismissed();
  };

  return (
    <div className="space-y-5">
      {/* Header */}
      <div>
        <h1 className="page-title text-3xl text-[var(--color-text-primary)]">
          Segurança
        </h1>
        <p className="mt-1 text-sm text-[var(--color-text-secondary)]">
          Vulnerabilidades nos seus repos GitHub e imagens Docker da VM.
        </p>
      </div>

      {/* Counters + Re-scan button */}
      <div className="flex flex-wrap items-center justify-between gap-3 rounded-lg border border-[var(--color-border-subtle)] bg-[var(--color-bg-card)] p-3">
        <div className="flex flex-wrap gap-3 text-sm">
          {summary ? (
            <>
              <SummaryCount label="Critical" count={summary.critical} color="danger" />
              <SummaryCount label="High" count={summary.high} color="warning" />
              <SummaryCount label="Medium" count={summary.medium} color="accent-secondary" />
              <SummaryCount label="Low" count={summary.low} color="text-muted" />
              {summary.last_scan && (
                <span className="text-xs text-[var(--color-text-muted)]">
                  · última coleta:{" "}
                  {new Date(summary.last_scan).toLocaleString("pt-BR")}
                </span>
              )}
            </>
          ) : (
            <span className="text-xs text-[var(--color-text-muted)]">carregando…</span>
          )}
        </div>
        <button
          onClick={handleScan}
          disabled={scanning || !ready}
          className="rounded-md bg-[var(--color-accent-primary)] px-3 py-1 text-xs font-semibold text-black transition hover:bg-[var(--color-accent-secondary)] disabled:opacity-50"
        >
          {scanning ? "Scanning…" : "🔄 Re-escanear agora"}
        </button>
      </div>

      {/* Filtros */}
      <div className="flex flex-wrap items-center gap-2 text-xs">
        <span className="text-[var(--color-text-secondary)]">Severidade:</span>
        {(["critical", "high", "medium", "low"] as VulnSeverity[]).map((sev) => (
          <FilterChip
            key={sev}
            label={SEV_LABEL[sev]}
            active={filters.severities.includes(sev)}
            onToggle={() => toggleSeverity(sev)}
          />
        ))}
        <span className="ml-3 text-[var(--color-text-secondary)]">Kind:</span>
        {(["deps", "image", "advisory"] as VulnKind[]).map((k) => (
          <FilterChip
            key={k}
            label={KIND_LABEL[k]}
            active={filters.kinds.includes(k)}
            onToggle={() => toggleKind(k)}
          />
        ))}
      </div>

      {/* Lista agrupada */}
      {vulnsError && !vulns && (
        <div className="rounded-lg border border-[var(--color-danger)]/40 bg-[var(--color-danger-soft)] p-3 text-xs text-[var(--color-danger)]">
          Erro: {String(vulnsError)}
        </div>
      )}

      {!vulns && (
        <InlineLoading
          minHeight="9rem"
          messages={[
            "Lendo vulnerabilidades",
            "Calculando dedup por CVE",
            "Cruzando com dismisseds",
            "Quase lá",
          ]}
        />
      )}

      {grouped && grouped.length === 0 && (
        <div className="rounded-2xl border border-dashed border-[var(--color-border-default)] p-6 text-center text-sm text-[var(--color-text-secondary)]">
          🎉 Nenhuma vulnerabilidade ativa nos filtros atuais.
        </div>
      )}

      {grouped && grouped.length > 0 && (
        <div className="space-y-4">
          {grouped.map(([source, list]) => (
            <section key={source} className="space-y-2">
              <h3 className="font-mono text-sm font-semibold text-[var(--color-text-primary)]">
                {source}{" "}
                <span className="text-[var(--color-text-muted)]">
                  ({list.length})
                </span>
              </h3>
              <div className="space-y-1.5">
                {list.map((v) => {
                  const key = vulnDismissKey(v);
                  return (
                    <VulnerabilityRow
                      key={key + v.last_seen}
                      vuln={v}
                      isDismissed={key in dismissed}
                      onDismiss={() => handleDismiss(v)}
                      onUndismiss={() => handleUndismiss(v)}
                    />
                  );
                })}
              </div>
            </section>
          ))}
        </div>
      )}

      <SecurityScanProgress
        scanning={scanning}
        onClose={() => setScanning(false)}
      />
    </div>
  );
}

function severityRank(s: string): number {
  const order: Record<string, number> = {
    critical: 0,
    high: 1,
    medium: 2,
    low: 3,
    unknown: 4,
  };
  return order[s] ?? 5;
}

function SummaryCount({
  label,
  count,
  color,
}: {
  label: string;
  count: number;
  color: "danger" | "warning" | "accent-secondary" | "text-muted";
}) {
  return (
    <span className="font-mono">
      <span className="text-[var(--color-text-muted)]">{label}:</span>{" "}
      <span style={{ color: `var(--color-${color})` }}>{count}</span>
    </span>
  );
}

function FilterChip({
  label,
  active,
  onToggle,
}: {
  label: string;
  active: boolean;
  onToggle: () => void;
}) {
  return (
    <button
      onClick={onToggle}
      className={
        "rounded-full border px-2 py-0.5 text-[10px] font-mono transition " +
        (active
          ? "border-[var(--color-accent-primary)] bg-[var(--color-accent-primary)]/15 text-[var(--color-accent-primary)]"
          : "border-[var(--color-border-subtle)] text-[var(--color-text-muted)] hover:text-[var(--color-text-secondary)]")
      }
    >
      {label}
    </button>
  );
}
```

- [ ] **Step 2: Type-check**

```bash
pnpm exec tsc --noEmit 2>&1 | tail -3
```

- [ ] **Step 3: Commit**

```bash
git add src/components/SecurityTab.tsx
git commit -m "feat(security): SecurityTab — orquestrador da aba Segurança

- Header com title + counters por severidade + botão Re-escanear
- Filtros: severidade (chips toggle) + kind (chips toggle), default Critical+High
- Lista agrupada por source_id, ordenada por severidade dentro de cada
- Dismiss/Undismiss persistente via config.rs com revalidação automática
  quando fix_version muda (shouldRevalidateDismiss)
- Empty state '🎉 Nenhuma vulnerabilidade ativa'
- Polling 60s + scan on-demand integrado com SecurityScanProgress overlay"
```

### Task C6: Integração no `App.tsx` + `ProjectCard.tsx`

**Files:**
- Modify: `src/App.tsx`
- Modify: `src/components/ProjectCard.tsx`

- [ ] **Step 1: Topbar nova com aba "Segurança"**

```bash
grep -n "TopView\|topView\|VM\|Skills" src/App.tsx | head -15
```

Edita `src/App.tsx`. Procura `type TopView` e adiciona `"security"`:

```typescript
type TopView = "projects" | "skills" | "vm" | "security";
```

Procura o `setTopView("vm")` ou `topView === "vm"` no JSX da topbar e adiciona botão pra security entre Skills e VM:

```tsx
<button
  onClick={() => setTopView("security")}
  className={topView === "security" ? topActive : topInactive}
>
  Segurança
</button>
```

(Ajusta `topActive`/`topInactive` ao pattern existente do projeto. Olha como Skills/VM estilizam.)

E adiciona render condicional:

```tsx
{topView === "security" && <SecurityTab />}
```

Import:
```typescript
import { SecurityTab } from "./components/SecurityTab";
```

- [ ] **Step 2: State `vulnCountByRepo` + polling pros chips**

Em `App.tsx`, adiciona:

```typescript
const [vulnCountByRepo, setVulnCountByRepo] = useState<Record<string, number>>({});

useEffect(() => {
  let cancelled = false;
  const fetchCounts = () => {
    monitorApi
      .vulnCountByRepo()
      .then((counts) => {
        if (!cancelled) setVulnCountByRepo(counts);
      })
      .catch(() => {
        // SSH tunnel pode estar fechado se aba VM não foi aberta —
        // tratamento silencioso.
      });
  };
  fetchCounts();
  const id = setInterval(fetchCounts, 5 * 60 * 1000); // 5min
  return () => {
    cancelled = true;
    clearInterval(id);
  };
}, []);
```

Import:
```typescript
import { monitorApi } from "./lib/monitor";
```

- [ ] **Step 3: Passa `vulnCount` pro ProjectCard**

No render dos `<ProjectCard>`, adiciona prop. Provavelmente no map dos projects:

```tsx
<ProjectCard
  // ...props existentes
  vulnCount={vulnCountByRepo[`Falkzera/${project.name}`] ?? 0}
/>
```

> **Nota:** chave do dict é `Falkzera/<repo>` (full_name vindo do GitHub). Project local tem nome simples — convenção: o projeto local em `~/Projects/<X>` corresponde ao repo `Falkzera/<X>` no GitHub.

- [ ] **Step 4: ProjectCard renderiza chip**

Edita `src/components/ProjectCard.tsx`. Adiciona prop:

```typescript
interface Props {
  // ...existentes
  vulnCount?: number;
}
```

Import:
```typescript
import { SecurityChip } from "./SecurityChip";
```

E no JSX, no canto onde mora o ClaudeChip ou similar (adjacente):

```tsx
<SecurityChip count={vulnCount ?? 0} />
```

(Posiciona próximo ao ClaudeChip ou no header do card — escolher local que não polue.)

- [ ] **Step 5: Type-check + build smoke**

```bash
pnpm exec tsc --noEmit 2>&1 | tail -3
cargo build --manifest-path src-tauri/Cargo.toml 2>&1 | tail -3
```

- [ ] **Step 6: Commit**

```bash
git add src/App.tsx src/components/ProjectCard.tsx
git commit -m "feat(security): aba Segurança no topbar + chip nos ProjectCards

- TopView ganha 'security'; botão na topbar entre Skills e VM
- App.tsx: state vulnCountByRepo populado por polling 5min do
  monitorApi.vulnCountByRepo() (silently fails se tunnel fechado)
- ProjectCard.tsx: prop vulnCount opcional, render <SecurityChip>
  no header do card
- Convenção: projeto local em ~/Projects/<X> = repo Falkzera/<X>"
```

---

## Phase D — Validação smoke

### Task D1: Build release + reinstall + smoke manual

> **Falcão executa, eu valido com ele.**

- [ ] **Step 1: Build release + reinstall**

```bash
pnpm tauri build --bundles deb,rpm 2>&1 | tail -3
rm -f ~/.local/bin/falcao-launcher
cp src-tauri/target/release/falcao-launcher ~/.local/bin/falcao-launcher
echo "binário reinstalado"
```

- [ ] **Step 2: Trigger primeiro scan completo**

```bash
gh workflow run security-scan.yml
gh run watch  # aguarda
```

E também:
```bash
ssh falcao@162.55.217.189 "~/.local/bin/scan-trivy.sh 2>&1 | tail -5"
```

- [ ] **Step 3: Smoke fluxos no launcher**

Falcão fecha launcher (`pkill -f /.local/bin/falcao-launcher`) e reabre via Activities. Valida:

1. **Aba "Segurança"** aparece no topbar entre Skills e VM
2. **Click na aba** → header com counters por severidade
3. **Última coleta** mostra timestamp recente (após Step 2)
4. **Lista agrupada por source_id** com pelo menos:
   - Repos `Falkzera/*` (kind=deps) se algum tem alerts
   - Imagens Docker (kind=image): `caddy:2-alpine`, `timescale/timescaledb-ha:pg16`, `falcao-financas-app`
5. **Filtros funcionam**: toggle "Medium" → CVEs Medium aparecem; toggle "Critical" → desaparecem
6. **Click "Dismissar"** num CVE → ele fica opaco (50%) ou some
7. **Click "Restaurar"** → volta normal
8. **Botão "🔄 Re-escanear agora"** → modal SecurityScanProgress aparece com mensagens cíclicas + tail do stderr do Trivy
9. **Aba Projetos** → `<SecurityChip>` vermelho aparece nos cards de projetos com CVE Critical/High aberto

- [ ] **Step 4: Verifica DB**

```bash
ssh falcao@162.55.217.189 "docker exec falcao-monitor-db psql -U postgres -d falcao_monitor -c \"SELECT kind, severity, count(*) FROM vulnerabilities WHERE state='open' AND ts > now() - interval '7 days' GROUP BY kind, severity ORDER BY kind, severity\""
```

Expected: rows agrupando por kind+severity.

- [ ] **Step 5: Reportar bugs (se houver) e iterar**

Se algo falhar, criar issues e endereçar antes de Phase E.

---

## Phase E — Docs + PR

### Task E1: Atualizar agent.md das pastas tocadas

**Files:**
- Modify: `src-tauri/src/.agent.md` (entrada de external.rs e config.rs)
- Modify: `src-tauri/src/monitor/.agent.md` (security.rs novo)
- Modify: `src/components/.agent.md` (4 componentes novos)
- Modify: `src/lib/.agent.md` (8 wrappers monitorApi novos)
- Modify: `src/types/.agent.md` (security.ts novo)

- [ ] **Step 1: Adicionar entries pra Sprint B1 em cada agent.md**

Pra cada arquivo, adicionar bullet point ou seção curta listando:
- Arquivos novos / modificações
- Decisões de design (1 linha)
- Referências a spec/plan

Exemplo pra `src/components/.agent.md`:

```markdown
### Sprint B1 — Snyk-like

- **`SecurityTab.tsx`** — aba "Segurança" no topbar. Counters + filtros + lista agrupada por source_id + dismiss persistente com revalidação por fix_version. Polling 60s.
- **`VulnerabilityRow.tsx`** — item individual: severity chip + ID + package + fix_version + link advisory + botão dismiss/restore.
- **`SecurityChip.tsx`** — chip compacto pros ProjectCards quando há CVE Critical/High aberto. Render null se count == 0.
- **`SecurityScanProgress.tsx`** — overlay com mensagens cíclicas + tail do stderr Trivy via evento Tauri 'vuln-scan-progress'.
- **Mods Sprint B1:** `ProjectCard` aceita prop `vulnCount?` e renderiza `<SecurityChip>`. `App.tsx` ganha aba 'security' + state `vulnCountByRepo`.
```

- [ ] **Step 2: Commit**

```bash
git add src-tauri/src/.agent.md src-tauri/src/monitor/.agent.md src/components/.agent.md src/lib/.agent.md src/types/.agent.md
git commit -m "docs(agent): Sprint B1 — Snyk-like nos agent.md das pastas tocadas"
```

### Task E2: Atualizar `CLAUDE.md`

- [ ] **Step 1: Adicionar seção após "Feature: Investigar com Claude (Sprint 4)"**

```markdown
## Feature: Snyk-like — vulnerabilidades cross-repo (Sprint B1)

Aba **"Segurança"** no topbar entre Skills e VM. Lista CVEs vindos de 3 fontes hybrid:
- **Dependabot alerts** dos repos `Falkzera/*` (auto-discovery)
- **Trivy** scans nas imagens Docker rodando na VM
- **GHSA cross-cutting** advisories que afetam packages dos teus repos

Coleta diária via cron (GH Actions pra deps/GHSA + systemd timer na VM pra Trivy) + botão "🔄 Re-escanear agora".

- **Spec:** `docs/superpowers/specs/2026-05-08-snyk-like-design.md`
- **Plan:** `docs/superpowers/plans/2026-05-08-snyk-like.md`
- **Migration:** `docs/superpowers/vm-migrations/008_vulnerabilities.sql`
- **GitHub PAT:** `GH_PAT_SECURITY` em GitHub Secrets + `~/.config/falcao-monitor/.env` na VM
- **Trivy install:** `curl -sfL .../install.sh | sudo sh -s -- -b /usr/local/bin` na VM (1x manual)
- **Reuso:** SSH key dedicada `falcao-monitor-push` da Sprint 2 health checks (mesma chave + secrets)

### Componentes novos (Sprint B1)
- `src/components/SecurityTab.tsx` — orquestrador da aba
- `src/components/VulnerabilityRow.tsx` — item individual
- `src/components/SecurityChip.tsx` — chip compacto pros ProjectCards
- `src/components/SecurityScanProgress.tsx` — overlay durante scan on-demand

### Backend novo
- `src-tauri/src/monitor/security.rs` — queries SELECT (list_vulnerabilities, vuln_summary, vuln_count_by_repo)
- `src-tauri/src/external.rs` — `trigger_trivy_scan_on_vm` (SSH + emit progress) e `trigger_dependabot_scan_via_gh` (gh workflow run)
- `src-tauri/src/config.rs` — `dismissed_vulnerabilities: HashMap<String, DismissedVuln>` em `~/.config/falcao-launcher/config.json` com revalidação por `fix_version`
```

- [ ] **Step 2: Commit**

```bash
git add CLAUDE.md
git commit -m "docs: CLAUDE.md ganhou seção Sprint B1 — Snyk-like"
```

### Task E3: Atualizar `VALIDATION.md`

- [ ] **Step 1: Adicionar seção ao final**

```bash
cat >> docs/superpowers/vm-migrations/VALIDATION.md <<'EOF'

---

## Sprint B1 — Snyk-like (2026-05-08)

Spec: `docs/superpowers/specs/2026-05-08-snyk-like-design.md`
Plan: `docs/superpowers/plans/2026-05-08-snyk-like.md`

### Acceptance criteria (15 itens)

- [x] Migration `008_vulnerabilities.sql` aplicada — hypertable + 2 indexes + compression 7d + retention 90d
- [x] Workflow `.github/workflows/security-scan.yml` rodando diário 06:07 UTC + workflow_dispatch
- [x] `scripts/scan-dependabot.sh` busca repos `Falkzera/*`, lista alerts + GHSA, push CSV pro Postgres via SSH
- [x] `/home/falcao/.local/bin/scan-trivy.sh` na VM + systemd timer ativo
- [x] Após primeira coleta: rows ≥ 0 no DB
- [x] Aba "Segurança" no topbar entre Skills e VM
- [x] Header da aba com counters por severidade + botão "Re-escanear agora"
- [x] Filtros por severidade (toggles) + kind (toggles) funcionando
- [x] Default mostra Critical+High; toggles ligam Medium/Low
- [x] Botão "Dismissar" persiste em config.json
- [x] Dismiss reaparece quando próximo scan trouxer fix_version diferente
- [x] `<SecurityChip>` aparece em ProjectCard quando repo tem CVE Critical/High aberto
- [x] "Re-escanear agora" dispara SSH (Trivy) + GH Actions (Dependabot) com progress streaming
- [x] cargo test + tsc + build release passando
- [x] Documentação completa (5 agent.md + CLAUDE.md + VALIDATION.md + skill)

### Observações operacionais

- **Trivy install manual** uma única vez na VM (`curl ... | sudo sh`). DB do Trivy ~60 MB cacheado em `~/.cache/trivy`. Primeiro scan demora 2-5 min; subsequentes <30s.
- **GitHub PAT scopes:** `Dependabot alerts:read`, `Metadata:read`, `Contents:read`, `Actions:read+write`. Validade 1 ano — rotacionar quando expirar.
- **SSH key reuso:** mesma `falcao-monitor-push` da Sprint 2. Sem nova chave, sem novo `command="..."` — psql do `monitor_writer` aceita `\COPY` em qualquer tabela onde tem grant.
- **DISTINCT ON pra dedup:** queries usam `DISTINCT ON (kind, source_id, COALESCE(cve_id, ghsa_id, package_name))` — evita N rows quando scan repete persistindo o mesmo CVE.
- **Filter `ts > now() - 7 days`:** vulnerabilidades obsoletas (não vistas em scans recentes) somem da UI mesmo estando na hypertable de 90d.

### Phase B2 backlog reconhecido (próximas Sprints)

- **Push pra Telegram** quando CVE Critical aparece (depende de bot Telegram — Sprint futura)
- **Multi-org repos** — incluir `nor-noreason/*` via allowlist
- **Histórico/timeline de CVEs** no UI (DB já persiste)
- **Auto-fix bot** que abre PR de bump (Dependabot já faz nativamente — não duplicar)
EOF
echo "OK"
```

- [ ] **Step 2: Commit**

```bash
git add docs/superpowers/vm-migrations/VALIDATION.md
git commit -m "docs(validation): seção Sprint B1 — Snyk-like (15 acceptance criteria)"
```

### Task E4: Push branch + abrir PR pra main

- [ ] **Step 1: Push**

```bash
git push -u origin feature/snyk-like 2>&1 | tail -3
```

- [ ] **Step 2: Abrir PR**

```bash
gh pr create --base main --head feature/snyk-like \
  --title "feat(security): Sprint B1 — Snyk-like (vulnerabilidades cross-repo + imagens Docker)" \
  --body "$(cat <<'EOF'
## Summary

Aba **"Segurança"** no topbar entre Skills e VM. Lista CVEs vindos de 3 fontes hybrid: Dependabot alerts (Falkzera/*), Trivy scan nas imagens Docker da VM, e GHSA cross-cutting. Coleta diária via cron + on-demand. Dismiss persistente com revalidação por fix_version. Chip vermelho discreto nos ProjectCards quando repo tem Critical/High aberto.

## Spec & plan

- Spec: `docs/superpowers/specs/2026-05-08-snyk-like-design.md`
- Plan: `docs/superpowers/plans/2026-05-08-snyk-like.md`
- Validation: `docs/superpowers/vm-migrations/VALIDATION.md`

## O que entrou

### DB
- Migration `008_vulnerabilities.sql` — hypertable TimescaleDB com kind discriminator, retention 90d, compression 7d
- 2 indexes: lookup geral + parcial open-critical

### Coleta
- `scripts/scan-dependabot.sh` — gh api auto-discovery Falkzera/* + GHSA cross-cutting
- `scripts/push-vulnerabilities.sh` — pipe SSH command-restricted (reusa key da Sprint 2)
- `.github/workflows/security-scan.yml` — cron diário 06:07 UTC + workflow_dispatch
- `/home/falcao/.local/bin/scan-trivy.sh` na VM + systemd timer daily

### Backend Rust
- `src-tauri/src/monitor/security.rs` — list_vulnerabilities, vuln_summary, vuln_count_by_repo (DISTINCT ON dedup)
- `src-tauri/src/external.rs` — trigger_trivy_scan_on_vm (SSH + emit events) + trigger_dependabot_scan_via_gh (gh workflow run)
- `src-tauri/src/config.rs` — dismissed_vulnerabilities + 3 comandos
- 8 comandos novos no generate_handler

### Frontend
- 4 componentes novos: SecurityTab, VulnerabilityRow, SecurityChip, SecurityScanProgress
- Tipos em src/types/security.ts
- 8 wrappers monitorApi novos
- TopView ganha 'security'; ProjectCard aceita vulnCount; App.tsx polling 5min de vulnCountByRepo

## Setup operacional novo

- **GitHub PAT** `GH_PAT_SECURITY` (Dependabot alerts:read + Actions:read+write, all repos Falkzera) — em GitHub Secrets + ~/.config/falcao-monitor/.env na VM
- **Trivy install** na VM: `curl ... | sudo sh -s -- -b /usr/local/bin` (1x manual)
- Reuso de SSH key dedicada `falcao-monitor-push` (Sprint 2 health checks)

## Acceptance

15 critérios validados — ver VALIDATION.md.

## Test plan

- [x] migration aplicada via apply-vm-migrations.sh, idempotente
- [x] cargo test passing, cargo build sem warnings novos
- [x] tsc --noEmit clean
- [x] Build release sem warnings novos
- [x] Smoke manual: workflow dispatch + scan-trivy.sh, aba carrega lista, filtros, dismiss/restore, chip nos ProjectCards

## Phase B2 backlog reconhecido

- Push Telegram (depende de bot — Sprint futura)
- Multi-org repos (nor-noreason/*) via allowlist
- Timeline histórico de CVEs

🤖 Generated with [Claude Code](https://claude.com/claude-code)
EOF
)" 2>&1 | tail -3
```

Expected: URL do PR.

---

## Resumo das fases

| Phase | Escopo | Tempo estimado |
|---|---|---|
| **A1** | Migration 008 + apply | ~15 min |
| **A2** | scan-dependabot.sh + push-vulnerabilities.sh + workflow | ~45 min |
| **A3** | Trivy install + scan-trivy.sh + systemd timer (manual + scripts) | ~30 min |
| **B1** | security.rs (queries + tipos) | ~30 min |
| **B2** | 3 comandos Tauri | ~20 min |
| **B3** | external.rs trigger_* | ~30 min |
| **B4** | config.rs dismissed_vulnerabilities | ~20 min |
| **C1** | tipos TS + 8 wrappers monitorApi | ~20 min |
| **C2** | SecurityChip | ~10 min |
| **C3** | VulnerabilityRow | ~30 min |
| **C4** | SecurityScanProgress | ~30 min |
| **C5** | SecurityTab orquestrador | ~75 min |
| **C6** | App.tsx topbar + ProjectCard chip | ~30 min |
| **D** | Smoke manual | ~30 min |
| **E** | Docs + PR | ~45 min |
| **Total sequencial** | | **~7-8h** |
| **Com paralelização** | A‖ inicia → B+C em paralelo | **~5-6h** |

## Paralelização recomendada

**Round 1 (sequencial):** A1 → A2 → A3 (cada um depende de validação manual do anterior)
**Round 2 (sequencial dentro):** B1 (tipos Rust) primeiro
**Round 3 (paralelo):** B2 ‖ B3 ‖ B4 ‖ Phase C inteira (após C1) — pastas disjuntas
**Round 4 (sequencial):** Phase D (Falcão valida) → Phase E (docs + PR)

Subagent A: Phase B (Rust completo após B1)
Subagent B: Phase C (Frontend completo após C1)

## Riscos de execução

1. **Trivy install requer sudo** — manual do Falcão na VM. Sem isso, Phase A3 não termina.
2. **GitHub PAT scopes errados** — workflow falha com 403. Validar via dry-run antes de cron.
3. **`gh api /advisories?ecosystem=...`** — endpoint pode mudar shape. Plan B: skip GHSA cross-cutting na primeira passada e adicionar depois.
4. **SSH key existing** — se a Sprint 2 health checks mudou alguma coisa na chave dedicada, push falha. Validar com `ssh -i ~/.ssh/falcao-monitor-push falcao-monitor-push@vm 'echo test'` antes de tudo.
5. **Trivy DB outdated** — primeira execução baixa ~60 MB; falha se VM sem rede. Aceito (raro).
