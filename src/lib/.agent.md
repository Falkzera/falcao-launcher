# lib — agent.md

## Propósito
Utilitários TypeScript puros (parsers, helpers de cálculo, formatadores) consumidos por componentes React. Sem JSX, sem efeitos — funções determinísticas testáveis isolado.

## Arquivos
- `claudeCost.ts` — cálculo de custo equivalente de sessões Claude Code (usado no `ClaudeTab`/`TokensChart`).
- `monitor.ts` — helpers + cliente Tauri do VM Monitor:
  - `monitorApi.*` — wrappers de cada `invoke()` (vmStatus, listContainers, fetchMetricSeries, fetchLogs, healthSummary, **listStacks**, **stackDetail**).
  - `useTunnel()` — hook lifecycle do SSH tunnel (open on mount, close on unmount).
  - `usePolling(fn, ms, enabled)` — hook genérico de polling com cleanup.
- `format.ts` — `fmtBytes`, `formatRelative`, `pickUptimeColor`, `pickProgressColor`.
- `metrics.ts` — `toRate` (deriva taxa MB/s de counters cumulativos).
- `cost.ts` — `forecastMonthly` (extrapola custo Hetzner pro mês corrente).

## Padrões
- Funções puras quando possível.
- Testes (quando houver) ficam ao lado com sufixo `.test.ts`.
- Não importar de `components/` — dependência só anda no sentido contrário.

## Decisões recentes
- 2026-05-06: `monitor.ts` adicionado pra Fase 1 do VM Monitor.
- 2026-05-07 (Sprint 2): `monitorApi` ganhou `listStacks()` e `stackDetail(name)` pras "Stacks em produção".
- 2026-05-07 (Sprint 3 — modo análise):
  - `useAnalysisLayouts.ts` novo — CRUD de layouts em localStorage (`analysis:layouts:v1`). Schema validation tolerante (corrompido recria, versão futura recusa, layouts individuais corrompidos descartados). save/update/delete/setCurrent/duplicate + exportLayout/importLayout JSON. Sem zod — type-guards manuais.
  - `useAnalysisContext.ts` novo — useMemo agregador. Recebe state do AnalysisPage, retorna `AnalysisContext` serializável (range, charts, logs, layout). Sprint 4 / Claude vai consumir.
  - `monitorApi.fetchLogsRange(container, sinceIso, untilIso)` — wrapper sobre comando Rust `monitor_fetch_logs_range`.
- 2026-05-07 (Sprint 4 — integração Claude):
  - `serializeAnalysis.ts` novo — `serializeContextToMarkdown(context, question)` gera prompt Markdown estruturado (Contexto / Métricas em CSV / Logs / Pergunta). Truncamento defensivo: 1000 pontos por chart, 200KB de logs. `estimatePromptSize()` pra preview no modal.
  - `resolveTargetDir.ts` novo — auto-detect do diretório onde Claude Code abre. `container.resource` → `~/Projects/<name>`; `vm`/`hetzner` → `~/Projects/falcao-launcher`. Rust faz fallback se path inexistente.
  - `monitorApi.spawnClaudeInvestigation(promptMarkdown, targetDir)` — wrapper sobre comando Rust `spawn_claude_investigation`.
- 2026-05-08 (Polish A — quick wins):
  - `format.ts` ganhou `fmtBytesPerSec(bps)` adaptativo (B/s, KB/s, MB/s, GB/s) e `formatByMetricName(metric, value)` (detecta sufixo `_bytes`/`_pct`/`load_*`/`cost_*`/`_ms` e formata adequadamente — usado no AnalysisChartSlot YAxis).
  - `serializeAnalysis.ts`: `SerializationMode = "raw" | "summary"` (toggle no modal Claude reduz prompt ~93-98%). 6 patterns redact tokens sensíveis em logs (Bearer/api_key/AWS/Anthropic/JWT/hex). Sempre ligado.
- 2026-05-08 (Sprint B1 — Snyk-like):
  - `monitorApi` ganhou 8 wrappers: `listVulnerabilities(filters)`, `vulnSummary()`, `vulnCountByRepo()`, `triggerTrivyScan()`, `triggerDependabotScan()`, `dismissCve(key, fixVersion)`, `undismissCve(key)`, `listDismissedCves()`.
- 2026-05-08 (Sprint B3): `monitorApi.costSummary()` e `monitorApi.costHistory(service, metric, sinceIso, untilIso)` adicionados.

## Pendências conhecidas
- Sem testes unitários ainda; parsers de bytes têm cobertura via uso real do dashboard.
