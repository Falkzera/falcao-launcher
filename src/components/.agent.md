# src/components/

Componentes React específicos do launcher. **Sem biblioteca de UI** (não usamos shadcn-CLI nem nada) — cada componente é escrito à mão, simples, ~30-200 linhas. Variants Framer Motion compartilhados ficam em `../styles/animations.ts`.

## Arquivos

- **`ProjectCard.tsx`** — card de um projeto no grid:
  - Avatar (favicon real do projeto OU iniciais sobre cor gerada por hash do nome)
  - Nome (Plus Jakarta semibold) + path (JetBrains Mono `text-muted`)
  - Indicador de status (idle/running/crashed) com pulse animado
  - Chip do package manager + script detectado, chip de porta clicável
  - Ícones de ação no hover: olho (esconder), engrenagem (configurar)
  - Sempre visíveis: VSCode, Ghostty (`<SystemIcon>`), Run/Stop
  - **`motion.div` com `cardHover`** (scale 1.005, y -2, spring) + glass (`bg-bg-card` + `backdrop-blur-md`)
  - **Botão Run/Stop tem radial reveal**: `AnimatePresence` com `clip-path: circle(0% → 150%)` quando estado muda — feedback colors NÃO podem pop, sempre via reveal.
  - **Visual de oculto** quando `project.hidden`: `border-dashed`, opacity 60%, saturate 50%, chip "OCULTO" no canto, hover restaura.

- **`LogsDrawer.tsx`** — drawer lateral direito (480px):
  - **`motion.aside` com `slideInRight`** (spring 320/32) — entra/sai animado.
  - Header: bolinha de status, nome do projeto (`.page-title`), chip da porta, botões `clear` e `✕`.
  - Body: lista de linhas com cor diferente pra stderr (vermelho suave).
  - Auto-scroll com sticky bottom: `stickyRef` desliga o scroll automático se o usuário rolar pra cima.
  - Footer: contador de linhas.

- **`ProjectConfigModal.tsx`** — modal de configuração por projeto:
  - **`motion.div` com `modalVariants` + `overlayVariants`**.
  - Inputs: porta frontend + porta backend (mono, 1024-65535).
  - Seção **Logo do projeto**: chama `list_icon_candidates` no Rust, exibe grid de thumbnails clicáveis dos candidatos detectados (4 colunas). Thumbnail ativo tem ring laranja. Botão "voltar ao auto" limpa o override.
  - Empty state quando não há candidatos com mensagem amigável.
  - Salva via `set_project_config` (commando Rust). Chama `onSaved?.()` pra `App.tsx` re-scanear.

- **`AddProjectModal.tsx`** — modal pra adicionar projeto por path arbitrário:
  - Input de texto, autoFocus, Enter pra submeter.
  - Chama `add_extra_path` no Rust (canonicaliza, valida que é dir).
  - `onAdded` re-fetcha a lista no parent.

- **`Checkbox.tsx`** — checkbox custom seguindo padrão Falcão System Design:
  - `bg-bg-tertiary` unchecked, `bg-accent-primary` checked.
  - **Animação assinatura**: ✓ desenhado em `pathLength: 0 → 1` em 0.22s + scale 0.5 → 1 + opacity. `whileTap: 0.88` pra feedback tátil.
  - `<input type="checkbox">` interno é `sr-only` (visualmente escondido).

- **`HealthChecksSection.tsx`** — section da aba VM que lista os health checks externos (3 endpoints públicos):
  - Recebe `enabled: boolean` (não abre tunnel próprio — VmTab já abriu).
  - Polling 30s via `usePolling(monitorApi.healthSummary, 30_000, enabled)`.
  - Loading state simples ("Carregando health checks…") + empty state quando todos `last_ts === null` (DB vazio antes do primeiro run do GH Actions).
  - Renderiza um `<HealthCheckRow>` por endpoint.

- **`HealthCheckRow.tsx`** — linha individual de health check:
  - StatusDot colorido (verde ok / vermelho down / cinza unknown).
  - Endpoint em font mono truncado, latência ou erro à direita, timestamp relativo via `formatRelative`.
  - Linha de pílulas de uptime (24h / 7d / 30d) com cor via `pickUptimeColor`.
  - Quando `last_ok === false`: borda 2px `--color-danger` + fundo levemente avermelhado.
  - `last_ok === null` (sem dados ainda) e `uptime_*h === null` rendem como "—" (text-tertiary).

### Sprint 2 — Vercel stacks

- **`StackGrid.tsx`** — grid 1-2 col das "Stacks em produção" na aba VM:
  - Polling `usePolling(monitorApi.listStacks, 30_000, enabled)`.
  - Estado `selectedStack: string | null` controla `<StackDrawer>` via `AnimatePresence`.
  - **Body scroll lock** (`document.body.style.overflow = "hidden"`) enquanto drawer aberto — evita scroll-chaining.
  - Esc fecha drawer.
  - Prop `showFrontendOnly` filtra visualmente stacks só-frontend (default oculto).
  - Empty state diferente quando filtro esconde tudo (call-to-action pra habilitar toggle).

- **`StackCard.tsx`** — card único de uma stack agregando 3 sub-blocos:
  - **Frontend Vercel:** `<VercelStatusBadge>` + tempo relativo do deploy + build_ms + chip de branch + commit_msg + autor + link clicável (`stopPropagation` pra não disparar drawer).
  - **Backend container:** `<UsageBar>` CPU% + RAM% + uptime. 3 estados de empty: "carregando…" / "container offline" / "sem backend na VM (só frontend Vercel)" — diferenciados via prop `hasContainerNames`.
  - **Endpoint público:** status code + latência + uptime 30d (vem dos health checks).
  - Card todo é clicável (cursor pointer, hover, focus ring, `role="button"`, tabIndex). Click chama `onOpen(stackName)`.

- **`StackDrawer.tsx`** — drawer lateral fullscreen pra detalhe da stack:
  - Polling lazy (`monitorApi.stackDetail(name)`, 30s).
  - **`<DrawerLoadingOverlay>`** sobre o conteúdo enquanto detail não chegou (primeira coleta). Polling subsequente NÃO reativa overlay.
  - 3 sections empilhadas: Vercel (último deploy + histórico expansível com 10 últimos), Backend container (UsageBar + charts CPU/RAM Recharts + logs on-demand reusando lógica do `VmContainerDrawer`), Endpoint público (status + uptime grid 24h/7d/30d).
  - **`overscrollBehavior: "contain"`** no scroll container.

- **`VercelStatusBadge.tsx`** — dot colorido + label de state Vercel:
  - READY → green, ERROR → red, BUILDING/QUEUED → amber pulsante (framer), CANCELED → muted.
  - Usado em StackCard e StackDrawer.

### Sprint 3 — Modo análise

- **`AnalysisPage.tsx`** — orquestrador. State global (preset, brushRange, charts, hoverTs, chartSeriesById, lastFetchedLogs). Renderizado pelo VmTab quando `vmView === "analysis"`. Chama `useAnalysisContext` (resultado descartado por enquanto — pra Sprint 4 / Claude consumir).
- **`AnalysisGrid.tsx`** — wrapper `<ResponsiveReactGridLayout>` com 3 breakpoints (lg=900px → 12 cols drag-drop, md=600px → 6 cols drag-drop, sm=0 → 1 col stack `static`). Persiste apenas o layout `lg` (single source of truth). Drag handle = `.analysis-slot-drag-handle` (header do AnalysisChartSlot).
- **`AnalysisChartSlot.tsx`** — slot do grid: MetricPicker no header (drag handle) + Recharts LineChart + Brush mini-overview. Re-fetch só quando preset/metric muda; brush não dispara fetch. Hover sync via `<ReferenceLine>` no `hoverTs`. Brush range marcado com 2 ReferenceLine verde (start/end).
- **`AnalysisLogsPanel.tsx`** — selectbox container + botão "Buscar logs do período". Bloqueia se range > 24h. Stale indicator quando range mudou pós-fetch. Truncated banner quando `response.truncated`. Comando: `monitorApi.fetchLogsRange()`.
- **`AnalysisLayoutPicker.tsx`** — header do AnalysisPage. Select de layouts salvos + Salvar como (form inline) + Renomear/Duplicar/Excluir/Export/Import. Surface error string do hook.
- **`MetricPicker.tsx`** — `<select>` agrupado por source (VM / Hetzner / Containers). Encoded value `kind:metric` ou `container:resource:metric`.
- **Mods Sprint 3:**
  - `VmMetricChart`: prop `onClick?` torna card clicável (cursor + hover + a11y `role=button`).
  - `VmContainerDrawer`: prop `onInvestigate?` renderiza botão "🔍 Investigar período" no header.
  - `StackDrawer` + `StackGrid`: prop `onInvestigateContainer?` propaga até o ContainerBlock; botão por container no bloco backend.
  - `VmTab`: state `vmView: { kind: "dashboard" } | { kind: "analysis", initialMetric, initialContainer }`. Renderiza `<AnalysisPage>` quando `analysis`. Helper `enterAnalysis(metric, container?)`.

### Sprint 4 — Integração Claude

- **`ClaudeInvestigationModal.tsx`** — modal pequeno chamado pelo header do `AnalysisPage`. Resumo do contexto (charts/range/logs/tamanho estimado) + textarea autofocus pra pergunta + botão "🚀 Spawnar Claude". Submit chama `serializeContextToMarkdown` + `monitorApi.spawnClaudeInvestigation`. Esc/overlay click fecha exceto durante loading. Erro inline (mantém modal pra retentar). Polish A: ganhou checkbox "Enviar resumo estatístico" pra reduzir prompt em ~93-98%.
- **Mods Sprint 4:**
  - `AnalysisPage.tsx`: `useAnalysisContext` agora **retorna** o context (Sprint 3 descartava). State `claudeModalOpen`, derivado `analysisReady` (todos charts populados). Botão "🤖 Investigar com Claude" no header (entre `← Voltar` e `<AnalysisLayoutPicker>`), disabled enquanto charts carregam.

### Sprint B1 — Snyk-like

- **`SecurityTab.tsx`** — orquestrador da aba Segurança. Header de counters por severidade + botão "🔄 Re-escanear agora" + filtros (toggles de severidade + kind) + lista agrupada por `source_id`. Polling 60s. Default filters: Critical+High. Dismiss persistente via `monitorApi.dismissCve()` com revalidação automática quando `fix_version` muda (`shouldRevalidateDismiss`).
- **`VulnerabilityRow.tsx`** — item individual da lista: severity chip colorido + ID (CVE/GHSA/package) + package@version + fix_version (verde com seta) + título + link advisory + botão Dismissar/Restaurar.
- **`SecurityChip.tsx`** — chip compacto pros `ProjectCard`s. Render `null` se `count == 0` (sem ruído).
- **`SecurityScanProgress.tsx`** — overlay durante scan on-demand. Listen no evento Tauri `vuln-scan-progress` (lines do stderr do scan-trivy). Reusa `<InlineLoading>` com 4 mensagens cíclicas custom.
- **Mods Sprint B1:**
  - `App.tsx`: `TopView` ganha `"security"`. Botão "Segurança" na topbar entre Skills e VM. Title "Segurança" + subtitle "CVEs nos seus repos e imagens da VM" no header padrão. State `vulnCountByRepo` populado por polling 5min de `monitorApi.vulnCountByRepo()`. Renderiza `<SecurityTab>` quando `topView === "security"`.
  - `ProjectCard.tsx`: prop `vulnCount?: number` opcional. Renderiza `<SecurityChip count={vulnCount}>` no cluster de chips do card.
  - `ProjectListItem.tsx`: aceita `vulnCount?` (não renderiza, apenas pra type-safety com spread props compartilhadas).

### Sprint B3 — Monitor de custos multi-serviço

- 2026-05-08 (Sprint B3): aba Custos completa. `CostTab` (orquestrador, polling 60s), `CostServiceCard` (card por serviço, lista métricas ordenadas por pct desc), `CostUsageBar` (barra horizontal com pctColor 4-state), `CostHistoryChart` (Recharts LineChart 30d, default = primeira métrica em alerta ou bandwidth Vercel), `CostChip` (topbar, render só quando dangerCount > 0).

- **`Loading.tsx`** — primitivos de loading reutilizáveis:
  - `<Spinner size="md|sm">` — SVG circular âmbar rotacionando 1.2s/volta.
  - `<LoadingMessages>` — mensagem ciclando (default 3s) + reticências animadas (1→2→3 pontos a cada 500ms).
  - `<DrawerLoadingOverlay loading messages?>` — overlay opaco (var(--color-bg-secondary) sólido, sem blur) sobre drawer enquanto fetch inicial.
  - `<InlineLoading minHeight messages?>` — placeholder centralizado pra grids/sections (substitui `animate-pulse` skeletons + textos "Carregando…" antigos).
  - **Aplicado em:** VmHeader, HealthChecksSection, StackGrid, VmContainerGrid, StackDrawer.

- **`SystemIcon.tsx`** — wrapper que usa `resolve_icon` do Rust pra pegar ícone do tema XDG ativo:
  - Cache estático (`Map`) por `name@size` — primeira render dispara fetch, próximas usam cache.
  - Fallback pra `<img src={fallback}>` quando o sistema não tem o ícone.
  - Usado pelos botões VSCode/Ghostty no card.

## Convenções

- **Props sempre tipadas com `type Props = { ... }`**, não `interface`. Mais fácil de estender com `&`.
- **`clsx` pra composição de classes condicionais.** Não usar `cn()` (não temos shadcn).
- **Não exportar default.** Usar named exports.
- **Stop propagation em handlers** quando o componente é clicável e tem buttons internos.
- **Não escrever lógica de negócio aqui.** Cards/modais/drawer só renderizam props e disparam callbacks. Estado e `invoke()` ficam no `App.tsx`. Exceções pontuais: botões Run/Stop, abrir VSCode, abrir Ghostty, ocultar/configurar — chamam `invoke` direto porque não impactam state externo.
- **Motion via Framer**: importar variants de `../styles/animations.ts`. NÃO criar variants ad-hoc dentro do componente — extrair pra reuso.
- **Tokens via classe Tailwind**: `bg-bg-secondary`, `text-text-primary`, `border-border-subtle`, `bg-accent-primary`, etc. Quando precisar de algo arbitrário (ex: shadow custom), usar `bg-[var(--color-bg-card)]`.
- **`page-title` pra display**: aplica Fraunces serif + letter-spacing. Usar em headers de modal/drawer e no título principal.
